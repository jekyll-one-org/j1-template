---
title:                                  SkipAd 
title_extention:                        Block YT Advertising
tagline:                                YouTube Video with No Ads

date:                                   2026-01-29
#last_modified:                         2026-01-01

description: >
                                        SkipAd, excellent online YouTube video player
                                        to block adverising. Blocks all ads instantly.
                                        Test YT Video with No Advertising

keywords: >
                                        open source, free, template, jekyll,
                                        jekyllone, web, sites, static, jamstack,
                                        bootstrap, html, html5, video, support,
                                        videojs

categories:                             [ Tester ]
tags:                                   [ Module, VideoJS ]

image:
  path:                                 /assets/image/page/skipad/skipad-promo.png
  width:                                1920
  height:                               1280

attic:                                  false
regenerate:                             false
personalization:                        true
permalink:                              /pages/public/tester/yt_skipad_tester/

resources:                              [ skipad, videojs ]
resource_options:
  - toccer:
      collapseDepth:                    3
  - attic:
      opacity:                          .6
      padding_top:                      550
      slides:
        - url:                          /assets/image/page/skipad/skipad-promo.png
          alt:                          VideoJS Tester
---

// Page Initializer
// =============================================================================
// Enable the Liquid Preprocessor
:page-liquid:


{% comment %} Set config files
-------------------------------------------------------------------------------- {% endcomment %}
{% assign template_config     = site.data.j1_config %}
{% assign blocks              = site.data.blocks %}
{% assign modules             = site.data.modules %}

{% comment %} Set config data (settings only)
------------------------------------------------------------------------------- {% endcomment %}
{% assign videojs_defaults    = modules.defaults.videojs.defaults %}
{% assign videojs_settings    = modules.videojs.settings %}

{% comment %} Set config options (settings only)
------------------------------------------------------------------------------- {% endcomment %}
{% assign videojs_options     = videojs_defaults | merge: videojs_settings %}


// Set (local) page attributes here
// -----------------------------------------------------------------------------
// :page--attr:                         <attr-value>

// Attribute settings for section control
//
:badges-enabled:                        false
:history-enabled:                       false

//  Load Liquid procedures
// -----------------------------------------------------------------------------
{% capture load_attributes %}themes/{{site.template.name}}/procedures/global/attributes_loader.proc{%endcapture%}

// Load page attributes
// -----------------------------------------------------------------------------
{% include {{load_attributes}} scope="global" %}

ifeval::[{badges-enabled} == true]

// ++++
// <span class="powered-by">Powered by J1 Template</span>
// ++++

[role="mb-5"]
{badge-j1--version-latest} {badge-j1--downloads}

endif::[]


// Page content
// ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
[role="dropcap"]
SkipAd, excellent online YouTube video player to block adverising. Blocks
all ads instantly - no more pre-roll, mid-roll, or overlay ads.

SkipAd provides an 100% *free alternative* to *YouTube Premium* for watching
a video undisturbed.

[role="mt-5"]
== Video selection

Paste a YT video link (or the YT video ID) to the input element below.

// Include sub-documents (if any)
// https://skipcut.com/
// https://www.youtube.com/watch?v=-w37wr0b6KE
// -----------------------------------------------------------------------------

++++
<div class="input-wrapper mt-4">
  <div class="input-container">
    <button id="pasteButton" class="paste-btn-inside" title="Paste from clipboard">
      <i class="fas fa-paste"></i>
      <span>Paste</span>
    </button>
    <input type="text" id="videoUrl" placeholder="https://www.youtube.com/watch?v=xxxx.. or xxxx..">
  </div>
  <!-- button id="loadVideo" class="watch-adfree-btn">Watch Ad Free</button -->
</div>

<!-- div class="video-title">
  <i class="mdib mdib-video mdib-24px mr-2"></i>
  Hazel Brugger · Immer noch wach
</div -->

<!-- div class="mb-5">
   <video
    id="-w37wr0b6KE"
    class="video-js vjs-theme-uno"
    controls
    width="640"
    height="360"
    poster="//img.youtube.com/vi/D6iVKisliNg/maxresdefault.jpg" alt="Lea Kyle · All Performances in AGT 2021" 
    aria-label="Lea Kyle"
    data-setup='{
        "fluid" : true,
        "techOrder": [
            "youtube", "html5"
        ],
        "sources": [{
            "type": "video/youtube",
            "src": "//youtu.be/D6iVKisliNg"
        }],
        "controlBar": {
          "pictureInPictureToggle": false,
          "volumePanel": {
              "inline": false
          }
        }
     }'
  ></video>
</div -->

<!-- div class="mb-5">
   <video
    id="-w37wr0b6KE"
    class="video-js vjs-theme-uno"
    controls
    width="640"
    height="360"
    poster="//img.youtube.com/vi/-w37wr0b6KE/maxresdefault.jpg" alt="Hazel Brugger · Immer noch wach" 
    aria-label="Hazel Brugger · Immer noch wach"
    data-setup='{
        "fluid" : true,
        "techOrder": [
            "youtube", "html5"
        ],
        "sources": [{
            "type": "video/youtube",
            "src": "//youtu.be/-w37wr0b6KE"
        }],
        "controlBar": {
          "pictureInPictureToggle": false,
          "volumePanel": {
              "inline": false
          }
        }
     }'
  ></video>
</div -->


<div id="videoContainer" class="video-container mb-8">

  <div id="emptyPlayerOverlay" class="empty-player-overlay">
    <div class="empty-player-content">
      <img src="/assets/image/page/skipad/skipad-logo.png" style="margin-bottom: 2rem"alt="Promo Image" width="512" height="128">
      <h3 class="notoc" style="opacity: 0.5">Paste YouTube Link/ID above</h3>
      <h4 class="notoc" style="opacity: 0.5">or</h4>
      <h3 class="notoc" style="opacity: 0.5">Paste from Clipboard (Paste Button)</h3>
      <!-- h4 class="notoc">and</h4>
      <h4 class="notoc">Lean back and watch your video ad-free</h4 -->
    </div>
  </div>

</div>
++++


ifeval::[{history-enabled} == true]
[role="mt-4"]
== History

[role="mb-4"]
A Private and personql history of last videos watched is stored in your
browser Clear the cache to erase it anytime!

++++
<div id="videoHistory" class="history-list mb-8">
  <div class="history-card card-base" data-video-id="-w37wr0b6KE">
    <div class="history-thumb-wrapper">
      <img class="history-thumb" src="https://img.youtube.com/vi/-w37wr0b6KE/mqdefault.jpg" alt="Hazel Brugger - Immer noch wach (GANZES PROGRAMM)">
      <div class="history-play-overlay"><i class="fas fa-play"></i></div>
      <!-- div class="history-duration">1:05:42</div -->
    </div>
    <div class="history-info">
      <div class="history-title">Hazel Brugger - Immer noch wach (GANZES PROGRAMM)</div>
      <div class="history-channel">Hazel Brugger • now</div>
      <div class="history-meta"></div>
      <div class="history-actions"><button class="history-btn" title="Download" aria-label="Download video"><i class="fas fa-download"></i></button><button class="history-btn" title="Share" aria-label="Share"><i class="fas fa-share-alt"></i></button><button class="history-btn delete" title="Delete" aria-label="Delete from history"><i class="fas fa-trash"></i></button></div>
    </div>
  </div>
</div>
++++

endif::[]

++++
<script>

  var vjsDefaults;
  var vjsSettings;
  var vjsOptions;

  window.skipAdJS = (function () {
    // -------------------------------------------------------------------------
    // global variable settings
    // -------------------------------------------------------------------------

    // create settings object from module options
    vjsDefaults = $.extend({}, {{videojs_defaults | replace: 'nil', 'null' | replace: '=>', ':' }});
    vjsSettings = $.extend({}, {{videojs_settings | replace: 'nil', 'null' | replace: '=>', ':' }});
    vjsOptions  = $.extend(true, {}, vjsDefaults, vjsSettings);    

    Element.prototype.appendAfter = function (element) {
        element.parentNode.insertBefore(this, element.nextSibling);
    };

    String.prototype.contains = function (it) {
        return this.indexOf(it) !== -1;
    };

    // =========================================================================
    // helper functions
    // -------------------------------------------------------------------------

    // Funktion zum Erstellen des Video-Elements
    function createVideoElement(videoId, width = 640, height = 360) {
      // Video-Element erstellen
      const video = document.createElement('video');
      
      // Klassen setzen
      video.id = videoId;
      video.className = 'video-js vjs-theme-uno';
      
      // Attribute setzen
      video.setAttribute('controls', '');
      video.setAttribute('width', width);
      video.setAttribute('height', height);
      video.setAttribute('poster', `//img.youtube.com/vi/${videoId}/maxresdefault.jpg`);
      video.setAttribute('alt', 'Hazel Brugger · Immer noch wach');
      video.setAttribute('aria-label', 'Hazel Brugger · Immer noch wach');
      
      // data-setup Konfiguration
      const dataSetup = {
        fluid: true,
        techOrder: ['youtube', 'html5'],
        sources: [{
          type: 'video/youtube',
          src: `//youtu.be/${videoId}`
        }],
        controlBar: {
          pictureInPictureToggle: false,
          volumePanel: {
            inline: false
          }
        }
      };
      
      video.setAttribute('data-setup', JSON.stringify(dataSetup));
      
      return video;
    }

    function addVideoToContainer(videoId, containerSelector) {
        const container = document.getElementById(containerSelector);
        
        if (!container) {
            console.error('Container nicht gefunden!');
            return;
        }
        
        container.innerHTML = `
            <video
                id="${videoId}"
                class="video-js vjs-theme-uno"
                controls
                width="640"
                height="360"
                poster="//img.youtube.com/vi/${videoId}/maxresdefault.jpg"
                alt="Hazel Brugger · Immer noch wach"
                aria-label="Hazel Brugger · Immer noch wach"
                data-setup='{
                    "fluid": true,
                    "techOrder": ["youtube", "html5"],
                    "sources": [{
                        "type": "video/youtube",
                        "src": "//youtu.be/${videoId}"
                    }],
                    "controlBar": {
                        "pictureInPictureToggle": false,
                        "volumePanel": {
                            "inline": false
                        }
                    }
                }'
            ></video>
        `;
    }

    var skipadjs = {
      text: 'Download',
      title: 'Download with TubeNinja',
      target: '_blank',
      btnClass: 'btn-tubeninja',
      init: function () {
        var videoID  = '-w37wr0b6KE';

        // Video dem Container hinzufügen
        // addVideoToContainer(videoID, 'videoContainer');
      }
    };

    document.addEventListener("DOMContentLoaded", function (event) {
      skipAdJS.init();
    });

    return skipadjs;
  }());

  var dependencies_met_page_ready = setInterval (function (options) {
    var pageState   = $('#no_flicker').css("display");
    var pageVisible = (pageState == 'block') ? true : false;

    if (j1.getState() === 'finished' && pageVisible) {

      $(document).on('click', 'a.ts, a.share', function (e) {
        e.preventDefault();
        window.open($(this).attr('href'), '', "width=550,height=350");
      });

      $('#urlfield').on('paste', function () {
          setTimeout(function () {
              $('#videoUrl').submit();
          }, 100);
      });

      clearInterval(dependencies_met_page_ready);
    }
  }, 10);

  $(function() {

    // =========================================================================
    // create config objects from JSON data
    // -------------------------------------------------------------------------
    var videojsConfig           = vjsOptions;

    // =========================================================================
    // VideoJS player settings
    // -------------------------------------------------------------------------
    const vjsPlayerType         = 'ytp';
    const vjsPlaybackRates      = videojsConfig.playbackRates.values;

    // =========================================================================
    // VideoJS plugin settings
    // -------------------------------------------------------------------------
    const piAutoCaption         = videojsConfig.plugins.autoCaption;
    const piHotKeys             = videojsConfig.plugins.hotKeys;
    const piSkipButtons         = videojsConfig.plugins.skipButtons;
    const piZoomButtons         = videojsConfig.plugins.zoomButtons;

    // =========================================================================
    // helper functions
    // -------------------------------------------------------------------------

    // Funktion zum Erstellen des Video-Elements
    function createVideoElement(videoId, width = 640, height = 360) {
      // Video-Element erstellen
      const video = document.createElement('video');
      
      // Klassen setzen
      video.id = videoId;
      video.className = 'video-js vjs-theme-uno';
      
      // Attribute setzen
      video.setAttribute('controls', '');
      video.setAttribute('width', width);
      video.setAttribute('height', height);
      video.setAttribute('poster', `//img.youtube.com/vi/${videoId}/maxresdefault.jpg`);
      video.setAttribute('alt', 'Hazel Brugger · Immer noch wach');
      video.setAttribute('aria-label', 'Hazel Brugger · Immer noch wach');
      
      // data-setup Konfiguration
      const dataSetup = {
        fluid: true,
        techOrder: ['youtube', 'html5'],
        sources: [{
          type: 'video/youtube',
          src: `//youtu.be/${videoId}`
        }],
        controlBar: {
          pictureInPictureToggle: false,
          volumePanel: {
            inline: false
          }
        }
      };
      
      video.setAttribute('data-setup', JSON.stringify(dataSetup));
      
      return video;
    }

    function addCaptionAfterImage(imageSrc) {
      const image = document.querySelector(`img[src="${imageSrc}"]`);

      if (image) {
        // create div|caption container
        const newDiv = document.createElement('div');
        newDiv.classList.add('caption');
        newDiv.textContent = 'Horst Evers - Parkfest 2019 (radioeins)';
        // insert div|caption container AFTER the image
        image.parentNode.insertBefore(newDiv, image.nextSibling);
      } else {
        console.error(`Kein Bild mit src="${imageSrc}" gefunden.`);
      }
    }

    // =========================================================================
    // initialize the VideoJS player (on page ready)
    // -------------------------------------------------------------------------
    var dependencies_met_page_ready = setInterval(function(options) {
      var pageState = $('#content').css("display");
      var pageVisible = (pageState == 'block') ? true : false;
      var j1CoreFinished = (j1.getState() === 'finished') ? true : false;

      if (j1CoreFinished && pageVisible) {

        $('#urlfield').on('paste', function () {
          setTimeout(function () {
              $('#videoUrl').submit();
          }, 100);
        });

        var vjs_player = document.getElementById("-w37wr0b6KE");

        // add captions (on poster image)
        // addCaptionAfterImage('//img.youtube.com/vi/-w37wr0b6KE/maxresdefault.jpg');

        // scroll page to the players top position
        // ---------------------------------------------------------------------
        // vjs_player.addEventListener('click', function(event) {
        //   const targetDiv         = document.getElementById("-w37wr0b6KE");
        //   const targetDivPosition = targetDiv.offsetTop;
        //   var scrollOffset        = (window.innerWidth >= 720) ? -130 : -110;
        //   // scroll player to top position
        //   window.scrollTo(0, targetDivPosition + scrollOffset);
        // });// END EventListener 'click'

        clearInterval(dependencies_met_page_ready);
      }
    }, 10);

    // customize the yt player created
    // -------------------------------------------------------------------------
    var dependencies_met_vjs_player_exist = setInterval(function(options) {
      var vjsPlayerExist = document.getElementById("-w37wr0b6KE") ? true : false;

      if (vjsPlayerExist) {

        // apply player customization on 'player ready'
        videojs("-w37wr0b6KE").ready(function() {
          var vjsPlayer   = this;
          var controlBar  = vjsPlayer.controlBar.el();
          var playerEl    = vjsPlayer.el();

          // create custom controlContainer (progressControlSilder|time display elements)
          var customProgressContainer = vjsPlayer.controlBar.addChild('Component', {
            el: videojs.dom.createEl('div', {
              className: 'vjs-theme-uno  custom-progress-container'
            })
          });

          // move progressControlSlider in den neuen Container
          var progressControl = vjsPlayer.controlBar.progressControl;
          if (progressControl) {
            customProgressContainer.el().appendChild(progressControl.el());
          }

          // move remainingTimeDisplay BEFORE the progressControlSilder
          var remainingTimeDisplay = vjsPlayer.controlBar.remainingTimeDisplay;
          if (remainingTimeDisplay) {
            customProgressContainer.el().insertBefore(remainingTimeDisplay.el(), progressControl.el());
          }

          // move the durationDisplay AFTER the progressControlSilder
          var durationDisplay = vjsPlayer.controlBar.durationDisplay;
          if (durationDisplay) {
            customProgressContainer.el().appendChild(durationDisplay.el());
          }

          // add|skip playbackRates
          if (videojsConfig.playbackRates.enabled) {
            vjsPlayer.playbackRates(vjsPlaybackRates);
          }

          // add|skip skipButtons plugin
          if (piSkipButtons.enabled) {
            var backwardIndex = piSkipButtons.backward;
            var forwardIndex  = piSkipButtons.forwardIndex;

            // property 'surroundPlayButton' takes precendence
            //
            if (piSkipButtons.surroundPlayButton) {
                var backwardIndex = 0;
                var forwardIndex  = 1;
            }
            vjsPlayer.skipButtons({
              backwardIndex: backwardIndex,
              forwardIndex: forwardIndex,
              backward: piSkipButtons.backward,
              forward: piSkipButtons.forward,
            });
          }

          // set start position of current video (on play)
          // -------------------------------------------------------------------
          var appliedOnce = false;
          vjsPlayer.on("play", function() {
            var startFromSecond = new Date('1970-01-01T' + "00:00:00" + 'Z').getTime() / 1000;
            if (!appliedOnce) {
                vjsPlayer.currentTime(startFromSecond);
                appliedOnce = true;
            }
          });

        }); // eventListener on 'player ready'

        clearInterval(dependencies_met_vjs_player_exist);
      } // END if 'vjsPlayerExist'
    }, 10); // END 'dependencies_met_vjs_player_exist'
  }); // END 'document ready'

</script>
++++

++++
<style>

</style>
++++
