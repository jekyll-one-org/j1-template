[role="mt-5"]
[[swiper-api--modules]]
== Modules

// claude - Update swiper manual #1
// The detailed per-module sections below (Navigation parameters/methods/
// events, Pagination parameters/methods/events, Scrollbar, Autoplay,
// Effects, Thumbs, Zoom, Keyboard, Mousewheel, Virtual, Hash/History,
// Controller, A11y) still contain `lorem:sentences[...]` placeholders.
// They are kept inside the block-comment delimiters `////` until each
// section can be filled in with real beginner-friendly content.
//
Swiper is built from a *core* and a set of optional *modules*. Each
module adds a feature (navigation arrows, pagination dots, autoplay,
zoom, etc.) and can be turned on or off independently. The J1 bundle
(`swiper-bundle.js`) ships with all of the modules below pre-registered,
so you only need to enable them in the parameters.


[role="mt-4"]
[[swiper-api-modules-navigation]]
=== Navigation

Adds two *navigation buttons* ("previous" and "next") next to the
slider. Visitors click them to jump one slide back or forward. You can
style the buttons with CSS or replace them with your own elements.

[cols="2a,2a,8a", subs=+macros, options="header", width="100%", role="table-striped rable"]
|===
|Name |Type |Description

|navigation
|boolean \| NavigationOptions
|Object with navigation parameters or boolean `true` to enable with
default settings.

[source, js]
----
const swiper = new Swiper('.swiper', {
  navigation: {
    nextEl: '.swiper-button-next',
    prevEl: '.swiper-button-prev',
  },
});
----

|===


[role="mt-4"]
[[swiper-api-modules-navigation--parameters]]
==== Parameters

lorem:sentences[3]

[cols="3a,1a,1a,7a", subs=+macros, options="header", width="100%", role="table-striped rable"]
|===
|Name |Type |Default |Description

|`addIcons`
|boolean
|true
|Boolean property to add SVG icons to navigation buttons

|`disabledClass` |string
|'swiper-button-disabled' |CSS class name added to navigation button
when it becomes disabled

|`enabled` |boolean | |Boolean property to
use with breakpoints to enable/disable navigation on certain breakpoints

|`hiddenClass` |string
|'swiper-button-hidden' |CSS class name added to navigation button when
it becomes hidden

|`hideOnClick` |boolean |false |Toggle
navigation buttons visibility after click on Slider's container

|`lockClass` |string
|'swiper-button-lock' |CSS class name added to navigation button when it
is disabled

|`navigationDisabledClass`
|string |'swiper-navigation-disabled' |CSS class name added on swiper
container when navigation is disabled by breakpoint

|`nextEl` |HTMLElement \| CSSSelector \|
null |null |String with CSS selector or HTML element of the element that
will work like "next" button after click on it

|`prevEl` |HTMLElement \| CSSSelector \|
null |null |String with CSS selector or HTML element of the element that
will work like "prev" button after click on it

|===


[role="mt-4"]
[[swiper-api-modules-navigation--properties]]
==== Properties

lorem:sentences[3]

[cols="3a,2a,7a", subs=+macros, options="header", width="100%", role="table-striped rable"]
|===
|Name |Type |Description

|`swiper.navigation.nextEl`
|HTMLElement
|HTMLElement of "next" navigation button

|`swiper.navigation.prevEl`
|HTMLElement
|HTMLElement of "previous" navigation button

|===


[role="mt-4"]
[[swiper-api-modules-navigation--methods]]
==== Methods

lorem:sentences[3]

[cols="4a,8a", subs=+macros, options="header", width="100%", role="table-striped rable"]
|===
|Name |Description

|`swiper.navigation.destroy()`
|Destroy navigation

|`swiper.navigation.init()`
|Initialize navigation

|`swiper.navigation.update()`
|Update navigation buttons state (enabled/disabled)

|===


[role="mt-4"]
[[swiper-api-modules-navigation--events]]
==== Events

lorem:sentences[3]

[cols="2a,2a,8a", subs=+macros, options="header", width="100%", role="table-striped rable"]
|===
|Name |Type |Description

|`navigationHide`
|(swiper)
|Event will be fired on navigation hide

|`navigationNext`
|(swiper)
|Event will be fired on navigation next button click

|`navigationPrev`
|(swiper)
|Event will be fired on navigation prev button click

|`navigationShow`
|(swiper)
|Event will be fired on navigation show

|===


[role="mt-4"]
[[swiper-api-modules-navigation--css]]
==== CSS

lorem:sentences[3]

[source, css, role="noclip"]
----
{
  --swiper-navigation-size: 44px;
  --swiper-navigation-top-offset: 50%;
  --swiper-navigation-sides-offset: 10px;
  --swiper-navigation-color: var(--swiper-theme-color);
}
----


[role="mt-4"]
[[swiper-api-modules-pagination]]
=== Pagination

lorem:sentences[3]

[role="mt-4"]
[[swiper-api-modules-pagination--parameters]]
==== Parameters

lorem:sentences[3]

[cols="3a,1a,1a,7a", subs=+macros, options="header", width="100%", role="table-striped rable"]
|===
|Name |Type |Default |Description

|`pagination`
|boolean \| PaginationOptions
|none
|Object with pagination parameters or boolean `true` to enable with
default settings.

[source, js]
----
const swiper = new Swiper('.swiper', {
  pagination: {
    el: '.swiper-pagination',
    type: 'bullets',
  },
});
----

|`bulletActiveClass`
|string
|'swiper-pagination-bullet-active'
|CSS class name of currently active pagination bullet

|`bulletClass`
|string
|'swiper-pagination-bullet'
|CSS class name of single pagination bullet

|`bulletElement`
|string
|
|Defines which HTML tag will be used to represent single pagination
bullet. Only for `bullets` pagination type.

|`clickable`
|boolean
|false
|If `true` then clicking on pagination button will cause transition to
appropriate slide. Only for bullets pagination type

|`clickableClass`
|string
|'swiper-pagination-clickable'
|CSS class name set to pagination when it is clickable

|`currentClass`
|string
|'swiper-pagination-current'
|CSS class name of the element with currently active index
in "fraction" pagination

|`dynamicBullets`
|boolean
|false
|Good to enable if you use bullets pagination with a lot of slides. So
it will keep only few bullets visible at the same time.

|`dynamicMainBullets`
|number
|1
|The number of main bullets visible when `dynamicBullets` enabled.

|`el`
|HTMLElement \| CSSSelector \| null
|null
|String with CSS selector or HTML element of the container with
pagination

|`enabled`
|boolean
|
|Boolean property to use with breakpoints to enable/disable
pagination on certain breakpoints

|`formatFractionCurrent`
|function(number)
|
|format fraction pagination current number. Function
receives current number, and you need to return formatted value

|`formatFractionTotal`
|function(number)
|
|format fraction pagination total number. Function
receives total number, and you need to return formatted value

|`hiddenClass`
|string
|'swiper-pagination-hidden'
|CSS class name of pagination when it becomes inactive

|`hideOnClick`
|boolean
|true
|Toggle (hide/show) pagination container visibility after click
on Slider's container

|`horizontalClass`
|string
|'swiper-pagination-horizontal'
|CSS class name set to pagination in horizontal Swiper

|`lockClass`
|string
|'swiper-pagination-lock'
|CSS class name set to pagination when it is disabled

|`modifierClass`
|string
|'swiper-pagination-'
|The beginning of the modifier CSS class name that
will be added to pagination depending on parameters

|`paginationDisabledClass`
|string
|'swiper-pagination-disabled'
|CSS class name added on swiper container and pagination element
when pagination is disabled by breakpoint

|`progressbarFillClass`
|string
|'swiper-pagination-progressbar-fill'
|CSS class name of pagination progressbar fill element

|`progressbarOpposite`
|boolean
|false
|Makes pagination progressbar opposite to Swiper's
`direction` parameter, means vertical progressbar for horizontal
swiper direction and horizontal progressbar for vertical swiper
direction

|`progressbarOppositeClass`
|string
|'swiper-pagination-progressbar-opposite'
|CSS class name of pagination progressbar opposite

|`renderBullet`
|function(number, string)
|null 
|This parameter allows totally customize pagination bullets, you need to
pass here a function that accepts `index` number of pagination bullet
and required element class name (`className`). Only for `bullets`
pagination type

[source, js]
----
const swiper = new Swiper('.swiper', {
  //...
  pagination: {
    //...
    renderBullet: function (index, className) {
      return '<span class="' + className + '">' + (index + 1) + '</span>';
    },
  },
});
----

|`renderCustom`
|function(Swiper, number, number)
|null
|This parameter is required for `custom` pagination type where you
have to specify how it should be rendered.

[source, js]
----
const swiper = new Swiper('.swiper', {
  //...
  pagination: {
    //...
    renderCustom: function (swiper, current, total) {
      return current + ' of ' + total;
    },
  },
});
----

|`renderFraction`
|function(string, string)
|null
|This parameter allows to customize "fraction" pagination html. Only for
`fraction` pagination type

[source, js]
----
const swiper = new Swiper('.swiper', {
  //...
  pagination: {
    //...
    renderFraction: function (currentClass, totalClass) {
      return '<span class="' + currentClass + '"></span>' +
              ' of ' +
              '<span class="' + totalClass + '"></span>';
    },
  },
});
----

|`renderProgressbar`
|function(string)
|null
|This parameter allows to customize "progress" pagination. Only for
`progress` pagination type

[source, js]
----
const swiper = new Swiper('.swiper', {
  //...
  pagination: {
    //...
    renderProgressbar: function (progressbarFillClass) {
      return '<span class="' + progressbarFillClass + '"></span>';
    },
  },
});
----

|`totalClass`
|string
|'swiper-pagination-total'
|CSS class name of the element with total number of "snaps"
in "fraction" pagination

|`type`
|'bullets' \| 'fraction' \| 'progressbar' \| 'custom'
|bullets
|String with type of pagination. Can be `bullets`, `fraction`,
`progressbar` or `custom`

|`verticalClass`
|string
|'swiper-pagination-vertical'
|CSS class name set to pagination in vertical Swiper

|===


[role="mt-4"]
[[swiper-api-modules-pagination--properties]]
==== Properties

lorem:sentences[3]

[cols="4a,2a,6a", subs=+macros, options="header", width="100%", role="table-striped rable"]
|===
|Name |Type |Description

|`swiper.pagination.bullets`
|HTMLElement
|Array of pagination bullets HTML elements. To get specific slide
HTMLElement use `swiper.pagination.bullets[1]`.

`|HTMLElement`
|HTMLElement of pagination container element

|===


[role="mt-4"]
[[swiper-api-modules-pagination--methods]]
==== Methods

lorem:sentences[3]

[cols="4a,8a", subs=+macros, options="header", width="100%", role="table-striped rable"]
|===
|Name |Description

|`swiper.pagination.destroy()`
|Destroy pagination

|`swiper.pagination.init()`
|Initialize pagination

|`swiper.pagination.render()`
|Render pagination layout

|`swiper.pagination.update()`
|Update pagination state (enabled/disabled/active)

|===


[role="mt-4"]
[[swiper-api-modules-pagination--events]]
==== Events

lorem:sentences[3]

[cols="4a,2a,6a", subs=+macros, options="header", width="100%", role="table-striped rable"]
|===
|Name |Type |Description

|`paginationHide`
|(swiper)
|Event will be fired on pagination hide

|`paginationRender`
|(swiper, paginationEl)
|Event will be fired after pagination rendered

|`paginationShow`
|(swiper)
|Event will be fired on pagination show

|`paginationUpdate`
|(swiper, paginationEl)
|Event will be fired when pagination updated
|===


[role="mt-4"]
[[swiper-api-modules-pagination--css]]
==== CSS

lorem:sentences[3]

[source, css, role="noclip"]
----
{
  --swiper-pagination-color: var(--swiper-theme-color);
  --swiper-pagination-left: auto;
  --swiper-pagination-right: 8px;
  --swiper-pagination-bottom: 8px;
  --swiper-pagination-top: auto;
  --swiper-pagination-fraction-color: inherit;
  --swiper-pagination-progressbar-bg-color: rgba(0, 0, 0, 0.25);
  --swiper-pagination-progressbar-size: 4px;
  --swiper-pagination-bullet-size: 8px;
  --swiper-pagination-bullet-width: 8px;
  --swiper-pagination-bullet-height: 8px;
  --swiper-pagination-bullet-inactive-color: #000;
  --swiper-pagination-bullet-inactive-opacity: 0.2;
  --swiper-pagination-bullet-opacity: 1;
  --swiper-pagination-bullet-horizontal-gap: 4px;
  --swiper-pagination-bullet-vertical-gap: 6px;
}
----

////
[role="mt-4"]
=== Scrollbar

lorem:sentences[3]

[role="mt-4"]
==== Scrollbar Parameters

lorem:sentences[3]

[cols="3a,3a,3a,3a", subs=+macros, options="header", width="100%", role="table-striped rable"]
|===
|Name |Type |Default |Description
|link:#param-scrollbar[scrollbar] |boolean \| ScrollbarOptions | a|
Object with scrollbar parameters or boolean `true` to enable with
default settings.

[source, js]
----
const swiper = new Swiper('.swiper', {
  scrollbar: {
    el: '.swiper-scrollbar',
    draggable: true,
  },
});
----

|\{ | | |

|link:#param-scrollbar-dragClass[dragClass] |string
|'swiper-scrollbar-drag' |Scrollbar draggable element CSS class

|link:#param-scrollbar-dragSize[dragSize] |number \| 'auto' |auto |Size
of scrollbar draggable element in px

|link:#param-scrollbar-draggable[draggable] |boolean |false |Set to
`true` to enable make scrollbar draggable that allows you to control
slider position

|link:#param-scrollbar-el[el] |HTMLElement \| CSSSelector \| null |null
|String with CSS selector or HTML element of the container with
scrollbar.

|link:#param-scrollbar-enabled[enabled] |boolean | |Boolean property to
use with breakpoints to enable/disable scrollbar on certain breakpoints

|link:#param-scrollbar-hide[hide] |boolean |true |Hide scrollbar
automatically after user interaction

|link:#param-scrollbar-horizontalClass[horizontalClass] |string
|'swiper-scrollbar-horizontal' |CSS class name set to scrollbar in
horizontal Swiper

|link:#param-scrollbar-lockClass[lockClass] |string
|'swiper-scrollbar-lock' |Scrollbar element additional CSS class when it
is disabled

|link:#param-scrollbar-scrollbarDisabledClass[scrollbarDisabledClass]
|string |'swiper-scrollbar-disabled' |CSS class name added on swiper
container and scrollbar element when scrollbar is disabled by breakpoint

|link:#param-scrollbar-snapOnRelease[snapOnRelease] |boolean |false |Set
to `true` to snap slider position to slides when you release scrollbar

|link:#param-scrollbar-verticalClass[verticalClass] |string
|'swiper-scrollbar-vertical' |CSS class name set to scrollbar in
vertical Swiper

|} | | |
|===

[role="mt-4"]
==== Scrollbar Methods

lorem:sentences[3]

[cols=",,",]
|===
|Properties | |

|link:#prop-swiper-scrollbar-dragEl[swiper.scrollbar.dragEl]
|HTMLElement |HTMLElement of Scrollbar draggable handler element

|link:#prop-swiper-scrollbar-el[swiper.scrollbar.el] |HTMLElement
|HTMLElement of Scrollbar container element

|Methods | |

|link:#method-swiper-scrollbar-destroy[swiper.scrollbar.destroy()] |
|Destroy scrollbar

|link:#method-swiper-scrollbar-init[swiper.scrollbar.init()] |
|Initialize scrollbar

|link:#method-swiper-scrollbar-setTranslate[swiper.scrollbar.setTranslate()]
| |Updates scrollbar translate

|link:#method-swiper-scrollbar-updateSize[swiper.scrollbar.updateSize()]
| |Updates scrollbar track and handler sizes
|===

[role="mt-4"]
==== Scrollbar Events

lorem:sentences[3]

[cols=",,",subs=+macros, options="header",]
|===
|Name |Arguments |Description
|link:#event-scrollbarDragEnd[scrollbarDragEnd] |(swiper, event) |Event
will be fired on draggable scrollbar drag end

|link:#event-scrollbarDragMove[scrollbarDragMove] |(swiper, event)
|Event will be fired on draggable scrollbar drag move

|link:#event-scrollbarDragStart[scrollbarDragStart] |(swiper, event)
|Event will be fired on draggable scrollbar drag start
|===

[role="mt-4"]
==== Scrollbar CSS Custom Properties

lorem:sentences[3]

[source, css]
----
 {
  --swiper-scrollbar-border-radius: 10px;
  --swiper-scrollbar-top: auto;
  --swiper-scrollbar-bottom: 4px;
  --swiper-scrollbar-left: auto;
  --swiper-scrollbar-right: 4px;
  --swiper-scrollbar-sides-offset: 1%;
  --swiper-scrollbar-bg-color: rgba(0, 0, 0, 0.1);
  --swiper-scrollbar-drag-bg-color: rgba(0, 0, 0, 0.5);
  --swiper-scrollbar-size: 4px;
}
----

[role="mt-4"]
=== Autoplay

lorem:sentences[3]

[role="mt-4"]
==== Autoplay Parameters

lorem:sentences[3]

[cols="3a,3a,3a,3a", subs=+macros, options="header", width="100%", role="table-striped rable"]
|===
|Name |Type |Default |Description
|link:#param-autoplay[autoplay] |boolean \| AutoplayOptions | a|
Object with autoplay parameters or boolean `true` to enable with
default settings

[source, js]
----
const swiper = new Swiper('.swiper', {
 autoplay: {
   delay: 5000,
 },
});
----

|\{ | | |

|link:#param-autoplay-delay[delay] |number |3000 a|
Delay between transitions (in ms). If this parameter is not specified,
auto play will be disabled

If you need to specify different delay for specific slides you can do it
by using`data-swiper-autoplay` (in ms) attribute on slide.

[source, html]
----
<!-- hold this slide for 2 seconds -->
<div class="swiper-slide" data-swiper-autoplay="2000">
----

|link:#param-autoplay-disableOnInteraction[disableOnInteraction]
|boolean |true |Set to `false` and autoplay will not be disabled after
user interactions (swipes), it will be restarted every time after
interaction

|link:#param-autoplay-pauseOnMouseEnter[pauseOnMouseEnter] |boolean
|false |When enabled autoplay will be paused on pointer (mouse) enter
over Swiper container.

|link:#param-autoplay-reverseDirection[reverseDirection] |boolean |false
|Enables autoplay in reverse direction

|link:#param-autoplay-stopOnLastSlide[stopOnLastSlide] |boolean |false
|Enable this parameter and autoplay will be stopped when it reaches last
slide (has no effect in loop mode)

|link:#param-autoplay-waitForTransition[waitForTransition] |boolean
|true |When enabled autoplay will wait for wrapper transition to
continue. Can be disabled in case of using Virtual Translate when your
slider may not have transition

|} | | |
|===

[role="mt-4"]
==== Autoplay Methods

lorem:sentences[3]

[cols=",,",]
|===
|Properties | |

|link:#prop-swiper-autoplay-paused[swiper.autoplay.paused] |boolean
|Whether autoplay is paused

|link:#prop-swiper-autoplay-running[swiper.autoplay.running] |boolean
|Whether autoplay enabled and running

|link:#prop-swiper-autoplay-timeLeft[swiper.autoplay.timeLeft] |number
|If autoplay is paused, it contains time left (in ms) before transition
to next slide

|Methods | |

|link:#method-swiper-autoplay-pause[swiper.autoplay.pause()] | |Pause
autoplay

|link:#method-swiper-autoplay-resume[swiper.autoplay.resume()] | |Resume
autoplay

|link:#method-swiper-autoplay-start[swiper.autoplay.start()] | |Start
autoplay

|link:#method-swiper-autoplay-stop[swiper.autoplay.stop()] | |Stop
autoplay
|===

[role="mt-4"]
==== Autoplay Events

lorem:sentences[3]

[cols=",,",subs=+macros, options="header",]
|===
|Name |Arguments |Description
|link:#event-autoplay[autoplay] |(swiper) |Event will be fired when
slide changed with autoplay

|link:#event-autoplayPause[autoplayPause] |(swiper) |Event will be fired
on autoplay pause

|link:#event-autoplayResume[autoplayResume] |(swiper) |Event will be
fired on autoplay resume

|link:#event-autoplayStart[autoplayStart] |(swiper) |Event will be fired
in when autoplay started

|link:#event-autoplayStop[autoplayStop] |(swiper) |Event will be fired
when autoplay stopped

|link:#event-autoplayTimeLeft[autoplayTimeLeft] |(swiper, timeLeft,
percentage) |Event triggers continuously while autoplay is enabled. It
contains time left (in ms) before transition to next slide and
percentage of that time related to autoplay delay
|===

image:data:image/svg+xml;base64,[data:image/svg+xml;base64,]Looking
for more advanced effects?

https://uiinitiative.com/catalog/stories-slider[[.font-medium]#Stories
Slider#[.text-on-surface-variant .hidden .sm:inline]#- Instagram-style
stories with progress
bars#]https://uiinitiative.com/catalog/super-flow[[.font-medium]#Super
Flow#[.text-on-surface-variant .hidden .sm:inline]#- Dynamic triangle
fragment effect#]


[role="mt-4"]
=== Free Mode

lorem:sentences[3]

[role="mt-4"]
==== Free Mode Parameters

lorem:sentences[3]

[cols="3a,3a,3a,3a", subs=+macros, options="header", width="100%", role="table-striped rable"]
|===
|Name |Type |Default |Description
|link:#param-freeMode[freeMode] |boolean \| FreeModeOptions | a|
Enables free mode functionality. Object with free mode parameters or
boolean `true` to enable with default settings.

[source, js]
----
const swiper = new Swiper('.swiper', {
  freeMode: true,
});

const swiper = new Swiper('.swiper', {
  freeMode: {
    enabled: true,
    sticky: true,
  },
});
----

|\{ | | |

|link:#param-freeMode-enabled[enabled] |boolean |false |Whether the free
mode is enabled

|link:#param-freeMode-minimumVelocity[minimumVelocity] |number |0.02
|Minimum touchmove-velocity required to trigger free mode momentum

|link:#param-freeMode-momentum[momentum] |boolean |true |If enabled,
then slide will keep moving for a while after you release it

|link:#param-freeMode-momentumBounce[momentumBounce] |boolean |true |Set
to `false` if you want to disable momentum bounce in free mode

|link:#param-freeMode-momentumBounceRatio[momentumBounceRatio] |number
|1 |Higher value produces larger momentum bounce effect

|link:#param-freeMode-momentumRatio[momentumRatio] |number |1 |Higher
value produces larger momentum distance after you release slider

|link:#param-freeMode-momentumVelocityRatio[momentumVelocityRatio]
|number |1 |Higher value produces larger momentum velocity after you
release slider

|link:#param-freeMode-sticky[sticky] |boolean |false |Set to enabled to
enable snap to slides positions in free mode

|} | | |
|===

[role="mt-4"]
=== Grid

lorem:sentences[3]

[role="mt-4"]
==== Grid Parameters

lorem:sentences[3]

[cols="3a,3a,3a,3a", subs=+macros, options="header", width="100%", role="table-striped rable"]
|===
|Name |Type |Default |Description
|link:#param-grid[grid] |GridOptions | a|
Object with grid parameters to enable "multirow" slider.

[source, js]
----
const swiper = new Swiper('.swiper', {
  grid: {
    rows: 2,
  },
});
----

|\{ | | |

|link:#param-grid-fill[fill] |'row' \| 'column' |column a|
Can be `column` or `row`. Defines how slides should fill rows,
by column or by row

____
if used with loop mode make sure number of slides is even specified in
loop mode requirements, or enable `loopAddBlankSlides` parameter
____

|link:#param-grid-rows[rows] |number |1 |Number of slides rows, for
multirow layout

|} | | |
|===

[role="mt-4"]
=== Manipulation

Manipulation module adds useful Swiper methods to manipulate slides. It
makes sense to use it only with Swiper Core version, not intended to be
uses with Swiper React or Vue.

[role="mt-4"]
==== Manipulation Methods

lorem:sentences[3]

[width="100%",cols="34%,33%,33%",]
|===
|Methods | |

|link:#method-swiper-addSlide[swiper.addSlide([.text-orange]#index#&#44;
[.text-orange]#slides#)] | a|
Add new slides to the required index. slides could be HTMLElement or
HTML string with new slide or array with such slides, for example:

[source, js]
----
addSlide(1, '<div class="swiper-slide">Slide 10"</div>')

addSlide(1, [
 '<div class="swiper-slide">Slide 10"</div>',
 '<div class="swiper-slide">Slide 11"</div>'
]);
----

|link:#method-swiper-appendSlide[swiper.appendSlide([.text-orange]#slides#)]
| a|
Add new slides to the end. slides could be HTMLElement or HTML string
with new slide or array with such slides, for example:

[source, js]
----
appendSlide('<div class="swiper-slide">Slide 10"</div>')

appendSlide([
 '<div class="swiper-slide">Slide 10"</div>',
 '<div class="swiper-slide">Slide 11"</div>'
]);
----

|link:#method-swiper-prependSlide[swiper.prependSlide([.text-orange]#slides#)]
| a|
Add new slides to the beginning. slides could be HTMLElement or HTML
string with new slide or array with such slides, for example:

[source, js]
----
prependSlide('<div class="swiper-slide">Slide 0"</div>')

prependSlide([
 '<div class="swiper-slide">Slide 1"</div>',
 '<div class="swiper-slide">Slide 2"</div>'
]);
----

|link:#method-swiper-removeAllSlides[swiper.removeAllSlides()] | |Remove
all slides

|link:#method-swiper-removeSlide[swiper.removeSlide([.text-orange]#slideIndex#)]
| a|
Remove selected slides. slideIndex could be a number with slide index to
remove or array with indexes.

[source, js]
----
removeSlide(0); // remove first slide
removeSlide([0, 1]); // remove first and second slides
removeAllSlides();    // Remove all slides
----

|===

[role="mt-4"]
=== Parallax

Swiper supports parallax transition effects for swiper/slides nested
elements. There are two types of parallax elements supported:

* Direct child elements of `swiper`. Parallax effect for such elements
will depend on total slider progress. Useful for parallax backgrounds
* Slides child elements. Parallax effect for such elements will depend
on slide progress

To enable parallax effects you need to init Swiper with passed
`parallax:true` parameter and add one of the following (or mix)
attributes to required elements:

* `data-swiper-parallax` - enable transform-translate parallax
transition. This attribute may accept:
 `number` - value in px (as for title, subtitle in example above) to
move element depending on progress. In this case such element will be
moved on ± this value in px depending on slide position (next or
previous)
 `percentage` - (as for "parallax-bg") to move element depending on
progress and on its size. In this case such element will be moved on ±
this percentage of its size (width in horizontal direction, and height
in vertical direction) depending on slide position (next or previous).
So if element has 400px width and you specified
data-swiper-parallax="50%" then it will be moved on ± 200px
* `data-swiper-parallax-x` - same but for x-axis direction
* `data-swiper-parallax-y` - same but for y-axis direction
* `data-swiper-parallax-scale` - scale ratio of the parallax element
when it is in "inactive" (not on active slide) state
* `data-swiper-parallax-opacity` - opacity of the parallax element
when it is in "inactive" (not on active slide) state
* `data-swiper-parallax-duration` - custom transition duration for
parallax elements

[source, html]
----
<div class="swiper">
  <!-- Parallax background element -->
  <div
    class="parallax-bg"
    style="background-image:url(path/to/image.jpg)"
    data-swiper-parallax="-23%"
  ></div>
  <div class="swiper-wrapper">
    <div class="swiper-slide">
      <!-- Each slide has parallax title -->
      <div class="title" data-swiper-parallax="-100">Slide 1</div>
      <!-- Parallax subtitle -->
      <div class="subtitle" data-swiper-parallax="-200">Subtitle</div>
      <!-- And parallax text with custom transition duration -->
      <div
        class="text"
        data-swiper-parallax="-300"
        data-swiper-parallax-duration="600"
      >
        <p>Lorem ipsum dolor sit amet, ...</p>
      </div>
      <!-- Opacity parallax -->
      <div data-swiper-parallax-opacity="0.5">I will change opacity</div>
      <!-- Scale parallax -->
      <div data-swiper-parallax-scale="0.15">I will change scale</div>
    </div>
    ...
  </div>
</div>
----

[role="mt-4"]
==== Parallax Parameters

lorem:sentences[3]

[cols="3a,3a,3a,3a", subs=+macros, options="header", width="100%", role="table-striped rable"]
|===
|Name |Type |Default |Description
|link:#param-parallax[parallax] |boolean \| ParallaxOptions | a|
Object with parallax parameters or boolean `true` to enable with
default settings.

[source, js]
----
const swiper = new Swiper('.swiper', {
  parallax: true,
});
----

|\{ | | |

|link:#param-parallax-enabled[enabled] |boolean |false |Enable, if you
want to use "parallaxed" elements inside of slider

|} | | |
|===

image:data:image/svg+xml;base64,[data:image/svg+xml;base64,]Looking
for more advanced effects?

https://uiinitiative.com/catalog/super-flow[[.font-medium]#Super
Flow#[.text-on-surface-variant .hidden .sm:inline]#- Parallax triangle
fragments#]https://uiinitiative.com/catalog/shutters-slider[[.font-medium]#Shutters
Slider#[.text-on-surface-variant .hidden .sm:inline]#- Venetian blinds
parallax effect#]

[role="mt-4"]
=== Lazy Loading

Since version 9 Swiper doesn't have a specific lazy loading API, as it
relies on native browser lazy loading feature. To use lazy loading, we
just need to set `loading="lazy"` on images and add preloader element:

[source, html]
----
<div class="swiper">
  <div class="swiper-wrapper">
    <!-- Lazy image -->
    <div class="swiper-slide">
      <img src="path/to/picture-1.jpg" loading="lazy" />
      <div class="swiper-lazy-preloader"></div>
    </div>

    <!-- Lazy image with srcset -->
    <div class="swiper-slide">
      <img
        src="path/to/logo-small.png"
        srcset="path/to/logo-large.png 2x"
        loading="lazy"
      />
      <div class="swiper-lazy-preloader"></div>
    </div>
  </div>
</div>
----

As you see:

* Lazy image must have `loading="lazy"` attribute
* Add animated preloader spinner to slide which will be removed
automatically after image loaded:

[source, html]
----
<div class="swiper-lazy-preloader"></div>
----

Or white one for dark layout:

[source, html]
----
<div class="swiper-lazy-preloader swiper-lazy-preloader-white"></div>
----

[role="mt-4"]
=== Fade Effect

Be sure to have the `effect` param set to `fade` in order for this
to work.

Note that `crossFade` should be set to `true` in order to avoid
seeing content behind or underneath.

[role="mt-4"]
==== Fade Effect Parameters

lorem:sentences[3]

[cols="3a,3a,3a,3a", subs=+macros, options="header", width="100%", role="table-striped rable"]
|===
|Name |Type |Default |Description
|link:#param-fadeEffect[fadeEffect] |FadeEffectOptions | a|
Object with Fade-effect parameters

[source, js]
----
const swiper = new Swiper('.swiper', {
  effect: 'fade',
  fadeEffect: {
    crossFade: true
  },
});
----

|\{ | | |

|link:#param-fadeEffect-crossFade[crossFade] |boolean |false |Enables
slides cross fade

|} | | |
|===

[role="mt-4"]
=== Coverflow Effect

Be sure to have the `effect` param set to `coverflow` in order for
this to work.

[role="mt-4"]
==== Coverflow Effect Parameters

lorem:sentences[3]

[cols="3a,3a,3a,3a", subs=+macros, options="header", width="100%", role="table-striped rable"]
|===
|Name |Type |Default |Description
|link:#param-coverflowEffect[coverflowEffect] |CoverflowEffectOptions |
a|
Object with Coverflow-effect parameters.

[source, js]
----
const swiper = new Swiper('.swiper', {
  effect: 'coverflow',
  coverflowEffect: {
    rotate: 30,
    slideShadows: false,
  },
});
----

|\{ | | |

|link:#param-coverflowEffect-depth[depth] |number |100 |Depth offset in
px (slides translate in Z axis)

|link:#param-coverflowEffect-modifier[modifier] |number |1 |Effect
multiplier

|link:#param-coverflowEffect-rotate[rotate] |number |50 |Slide rotate in
degrees

|link:#param-coverflowEffect-scale[scale] |number |1 |Slide scale effect

|link:#param-coverflowEffect-slideShadows[slideShadows] |boolean |true
|Enables slides shadows

|link:#param-coverflowEffect-stretch[stretch] |number |0 a|
Stretch space between slides

* a number is interpreted as pixels (e.g., `20` for 20px).
* a string with a percentage (e.g., `"50%"`).

|} | | |
|===

image:data:image/svg+xml;base64,[data:image/svg+xml;base64,]Looking
for more advanced effects?

https://uiinitiative.com/catalog/carousel-slider[[.font-medium]#Carousel
Slider#[.text-on-surface-variant .hidden .sm:inline]#- True 3D infinite
carousel#]https://uiinitiative.com/catalog/super-flow[[.font-medium]#Super
Flow#[.text-on-surface-variant .hidden .sm:inline]#- Dynamic triangle
fragments#]


[role="mt-4"]
=== Flip Effect

Be sure to have the `effect` param set to `flip` in order for this
to work.

[role="mt-4"]
==== Flip Effect Parameters

lorem:sentences[3]

[cols="3a,3a,3a,3a", subs=+macros, options="header", width="100%", role="table-striped rable"]
|===
|Name |Type |Default |Description
|link:#param-flipEffect[flipEffect] |FlipEffectOptions | a|
Object with Flip-effect parameters

[source, js]
----
const swiper = new Swiper('.swiper', {
  effect: 'flip',
  flipEffect: {
    slideShadows: false,
  },
});
----

|\{ | | |

|link:#param-flipEffect-limitRotation[limitRotation] |boolean |true
|Limit edge slides rotation

|link:#param-flipEffect-slideShadows[slideShadows] |boolean |true
|Enables slides shadows

|} | | |
|===

[role="mt-4"]
=== Cube Effect

Be sure to have the `effect` param set to `cube` in order for this
to work.

[role="mt-4"]
==== Cube Effect Parameters

lorem:sentences[3]

[cols="3a,3a,3a,3a", subs=+macros, options="header", width="100%", role="table-striped rable"]
|===
|Name |Type |Default |Description
|link:#param-cubeEffect[cubeEffect] |CubeEffectOptions | a|
Object with Cube-effect parameters

[source, js]
----
const swiper = new Swiper('.swiper', {
  effect: 'cube',
  cubeEffect: {
    slideShadows: false,
  },
});
----

|\{ | | |

|link:#param-cubeEffect-shadow[shadow] |boolean |true |Enables main
slider shadow

|link:#param-cubeEffect-shadowOffset[shadowOffset] |number |20 |Main
shadow offset in px

|link:#param-cubeEffect-shadowScale[shadowScale] |number |0.94 |Main
shadow scale ratio

|link:#param-cubeEffect-slideShadows[slideShadows] |boolean |true
|Enables slides shadows

|} | | |
|===

[role="mt-4"]
=== Cards Effect

Be sure to have the `effect` param set to `cards` in order for
this to work.

[role="mt-4"]
==== Cards Effect Parameters

lorem:sentences[3]

[cols="3a,3a,3a,3a", subs=+macros, options="header", width="100%", role="table-striped rable"]
|===
|Name |Type |Default |Description
|link:#param-cardsEffect[cardsEffect] |CardsEffectOptions | a|
Object with Cards-effect parameters

[source, js]
----
const swiper = new Swiper('.swiper', {
  effect: 'cards',
  cardsEffect: {
    // ...
  },
});
----

|\{ | | |

|link:#param-cardsEffect-perSlideOffset[perSlideOffset] |number |8
|Offset distance per slide (in px)

|link:#param-cardsEffect-perSlideRotate[perSlideRotate] |number |2
|Rotate angle per slide (in degrees)

|link:#param-cardsEffect-rotate[rotate] |boolean |true |Enables cards
rotation

|link:#param-cardsEffect-slideShadows[slideShadows] |boolean |true
|Enables slides shadows

|} | | |
|===

image:data:image/svg+xml;base64,[data:image/svg+xml;base64,]Looking
for more advanced effects?

https://uiinitiative.com/catalog/tinder-slider[[.font-medium]#Tinder
Slider#[.text-on-surface-variant .hidden .sm:inline]#- Swipe direction
detection +
callbacks#]https://uiinitiative.com/catalog/cards-stack-slider[[.font-medium]#Cards
Stack Slider#[.text-on-surface-variant .hidden .sm:inline]#- Front/back
card sides#]


[role="mt-4"]
=== Creative Effect

Be sure to have the `effect` param set to `creative` in order for
this to work.

[role="mt-4"]
==== Creative Effect Parameters

lorem:sentences[3]

[cols="3a,3a,3a,3a", subs=+macros, options="header", width="100%", role="table-striped rable"]
|===
|Name |Type |Default |Description
|link:#param-creativeEffect[creativeEffect] |CreativeEffectOptions | a|
Object with Creative-effect parameters

[source, js]
----
const swiper = new Swiper('.swiper', {
  effect: 'creative',
  creativeEffect: {
    prev: {
      // will set `translateZ(-400px)` on previous slides
      translate: [0, 0, -400],
    },
    next: {
      // will set `translateX(100%)` on next slides
      translate: ['100%', 0, 0],
    },
  },
});
----

|\{ | | |

|link:#param-creativeEffect-limitProgress[limitProgress] |number |1
|Limit progress/offset to amount of side slides. If `1`, then slides
all slides after prev/next will have same state. If `2`, then all
slides after 2nd before/after active will have same state, etc.

|link:#param-creativeEffect-next[next] |CreativeEffectTransform | a|
Next slide transformations.

[source, js]
----
{
  // Array with translate X, Y and Z values
  translate: (string | number)[];
  // Array with rotate X, Y and Z values (in deg)
  rotate?: number[];
  // Slide opacity
  opacity?: number;
  // Slide scale
  scale?: number;
  // Enables slide shadow
  shadow?: boolean;
  // Transform origin, e.g. `left bottom`
  origin?: string;
}
----

|link:#param-creativeEffect-perspective[perspective] |boolean |true
|Enable this parameter if your custom transforms require 3D
transformations (`translateZ`, `rotateX`, `rotateY` )

|link:#param-creativeEffect-prev[prev] |CreativeEffectTransform | a|
Previous slide transformations. Accepts object of the following type:

[source, js]
----
{
  // Array with translate X, Y and Z values
  translate: (string | number)[];
  // Array with rotate X, Y and Z values (in deg)
  rotate?: number[];
  // Slide opacity
  opacity?: number;
  // Slide scale
  scale?: number;
  // Enables slide shadow
  shadow?: boolean;
  // Transform origin, e.g. `left bottom`
  origin?: string;
}
----

|link:#param-creativeEffect-progressMultiplier[progressMultiplier]
|number |1 |Allows to multiply slides transformations and opacity.

|link:#param-creativeEffect-shadowPerProgress[shadowPerProgress]
|boolean |false |Splits shadow "opacity" per slide based on
`limitProgress` (only if transformation shadows enabled). E.g. setting
`limitProgress: 2` and enabling `shadowPerProgress`, will set shadow
opacity to `0.5` and `1` on two slides next to active. With this
parameter disabled, all slides beside active will have shadow with `1`
opacity

|} | | |
|===

image:data:image/svg+xml;base64,[data:image/svg+xml;base64,]Looking
for more advanced effects?

https://uiinitiative.com/catalog/shaders-slider[[.font-medium]#Shaders
Slider#[.text-on-surface-variant .hidden .sm:inline]#- 20+ WebGL shader
transitions#]https://uiinitiative.com/catalog/super-flow[[.font-medium]#Super
Flow#[.text-on-surface-variant .hidden .sm:inline]#- Dynamic triangle
fragments#]https://uiinitiative.com/catalog/material-you-slider[[.font-medium]#Material
You Slider#[.text-on-surface-variant .hidden .sm:inline]#- Google
Material You design#]


[role="mt-4"]
=== Thumbs

In addition to link:#controller[Controller] component Swiper comes with
Thumbs component that is designed to work with additional thumbs swiper
in a more correct way than Controller which is used for syncing two
swipers.

[role="mt-4"]
==== Thumbs Parameters

lorem:sentences[3]

[cols="3a,3a,3a,3a", subs=+macros, options="header", width="100%", role="table-striped rable"]
|===
|Name |Type |Default |Description
|link:#param-thumbs[thumbs] |ThumbsOptions | a|
Object with thumbs component parameters

[source, js]
----
const swiper = new Swiper('.swiper', {
  ...
  thumbs: {
    swiper: thumbsSwiper
  }
});
----

|\{ | | |

|link:#param-thumbs-autoScrollOffset[autoScrollOffset] |number |0
|Allows to set on which thumbs active slide from edge it should
automatically move scroll thumbs. For example, if set to 1 and last
visible thumb will be activated (1 from edge) it will auto scroll thumbs

|link:#param-thumbs-multipleActiveThumbs[multipleActiveThumbs] |boolean
|true |When enabled multiple thumbnail slides may get activated

|link:#param-thumbs-slideThumbActiveClass[slideThumbActiveClass] |string
|'swiper-slide-thumb-active' |Additional class that will be added to
activated thumbs swiper slide

|link:#param-thumbs-swiper[swiper] |string \| Swiper \| null |'null'
|Swiper instance of swiper used as thumbs or object with Swiper
parameters to initialize thumbs swiper

|link:#param-thumbs-thumbsContainerClass[thumbsContainerClass] |string
|'swiper-thumbs' |Additional class that will be added to thumbs swiper

|} | | |
|===

[role="mt-4"]
==== Thumbs Methods

lorem:sentences[3]

[cols=",,",]
|===
|Properties | |

|link:#prop-swiper-thumbs-swiper[swiper.thumbs.swiper] |Swiper |Swiper
instance of thumbs swiper

|Methods | |

|link:#method-swiper-thumbs-init[swiper.thumbs.init()] | |Initialize
thumbs

|link:#method-swiper-thumbs-update[swiper.thumbs.update([.text-orange]#initial#&#44;
[.text-orange]#p#)] | |Update thumbs
|===


[role="mt-4"]
=== Zoom

Swiper supports zoom images functionality (similar to what you see on
iOS when browsing single photo) where you can zoom-in image by pinch
gesture and or by zoom-in/out by double tap on it. In this case,
additional layout is required:

[source, html]
----
<div class="swiper">
  <div class="swiper-wrapper">
    <div class="swiper-slide">
      <div class="swiper-zoom-container">
        <img src="path/to/image1.jpg" />
      </div>
    </div>
    <div class="swiper-slide">
      <div class="swiper-zoom-container">
        <img src="path/to/image2.jpg" />
      </div>
    </div>
    <div class="swiper-slide">Plain slide with text</div>
    <div class="swiper-slide">
      <!-- Override maxRatio parameter -->
      <div class="swiper-zoom-container" data-swiper-zoom="5">
        <img src="path/to/image1.jpg" />
      </div>
    </div>
  </div>
</div>
----

* All "zoomable" images should be wrapped with the div with
`swiper-zoom-container` class.
* By default it expects to zoom one of the `img`, `picture` or
`canvas` element. If you want to make zoom on some other custom
element, then just add `swiper-zoom-target` class to this element. For
example:
+
[source, html]
----
<div class="swiper">
  <div class="swiper-wrapper">
    <div class="swiper-slide">
      <div class="swiper-zoom-container">
        <!-- custom zoomable element -->
        <div
          class="swiper-zoom-target"
          style="background-image: url(...)"
        ></div>
      </div>
    </div>
  </div>
</div>
----
* You can override `maxRatio` parameter for specific slides by using
`data-swiper-zoom` attribute on zoom container.

[role="mt-4"]
==== Zoom Parameters

lorem:sentences[3]

[cols="3a,3a,3a,3a", subs=+macros, options="header", width="100%", role="table-striped rable"]
|===
|Name |Type |Default |Description
|link:#param-zoom[zoom] |boolean \| ZoomOptions | a|
Enables zooming functionality. Object with zoom parameters or boolean
`true` to enable with default settings

[source, js]
----
const swiper = new Swiper('.swiper', {
  zoom: {
    maxRatio: 5,
  },
});
----

|\{ | | |

|link:#param-zoom-containerClass[containerClass] |string
|'swiper-zoom-container' |CSS class name of zoom container

|link:#param-zoom-limitToOriginalSize[limitToOriginalSize] |boolean
|false |When set to true, the image will not be scaled past 100% of its
original size

|link:#param-zoom-maxRatio[maxRatio] |number |3 |Maximum image zoom
multiplier

|link:#param-zoom-minRatio[minRatio] |number |1 |Minimal image zoom
multiplier

|link:#param-zoom-panOnMouseMove[panOnMouseMove] |boolean |false |When
set to true, a zoomed in image will automatically pan while moving the
mouse over the image

|link:#param-zoom-toggle[toggle] |boolean |true |Enable/disable zoom-in
by slide's double tap

|link:#param-zoom-zoomedSlideClass[zoomedSlideClass] |string
|'swiper-slide-zoomed' |CSS class name of zoomed in container

|} | | |
|===

[role="mt-4"]
==== Zoom Methods

lorem:sentences[3]

[cols=",,",]
|===
|Properties | |

|link:#prop-swiper-zoom-enabled[swiper.zoom.enabled] |boolean |Whether
the zoom module is enabled

|link:#prop-swiper-zoom-scale[swiper.zoom.scale] |number |Current image
scale ratio

|Methods | |

|link:#method-swiper-zoom-disable[swiper.zoom.disable()] | |Disable zoom
module

|link:#method-swiper-zoom-enable[swiper.zoom.enable()] | |Enable zoom
module

|link:#method-swiper-zoom-in[swiper.zoom.in([.text-orange]#ratio#)] |
|Zoom in image of the currently active slide. Optionally accepts custom
zoom ratio

|link:#method-swiper-zoom-out[swiper.zoom.out()] | |Zoom out image of
the currently active slide

|link:#method-swiper-zoom-toggle[swiper.zoom.toggle([.text-orange]#event#)]
| |Toggle image zoom of the currently active slide
|===

[role="mt-4"]
==== Zoom Events

lorem:sentences[3]

[cols=",,",subs=+macros, options="header",]
|===
|Name |Arguments |Description
|link:#event-zoomChange[zoomChange] |(swiper, scale, imageEl, slideEl)
|Event will be fired on zoom change
|===


[role="mt-4"]
=== Keyboard Control

lorem:sentences[3]

[role="mt-4"]
==== Keyboard Control Parameters

lorem:sentences[3]

[cols="3a,3a,3a,3a", subs=+macros, options="header", width="100%", role="table-striped rable"]
|===
|Name |Type |Default |Description
|link:#param-keyboard[keyboard] |boolean \| KeyboardOptions | a|
Enables navigation through slides using keyboard. Object with keyboard
parameters or boolean `true` to enable with default settings

[source, js]
----
const swiper = new Swiper('.swiper', {
  keyboard: {
    enabled: true,
    onlyInViewport: false,
  },
});
----

|\{ | | |

|link:#param-keyboard-enabled[enabled] |boolean |false |Set to `true`
to enable keyboard control

|link:#param-keyboard-onlyInViewport[onlyInViewport] |boolean |true
|When enabled it will control sliders that are currently in viewport

|link:#param-keyboard-pageUpDown[pageUpDown] |boolean |true |When
enabled it will enable keyboard navigation by Page Up and Page Down keys

|link:#param-keyboard-speed[speed] |number |undefined |Set the speed of
keyboard navigation transitions (in ms)

|} | | |
|===

[role="mt-4"]
==== Keyboard Control Methods

lorem:sentences[3]

[cols=",,",]
|===
|Properties | |

|link:#prop-swiper-keyboard-enabled[swiper.keyboard.enabled] |boolean
|Whether the keyboard control is enabled

|Methods | |

|link:#method-swiper-keyboard-disable[swiper.keyboard.disable()] |
|Disable keyboard control

|link:#method-swiper-keyboard-enable[swiper.keyboard.enable()] | |Enable
keyboard control
|===

[role="mt-4"]
==== Keyboard Events

lorem:sentences[3]

[cols=",,",subs=+macros, options="header",]
|===
|Name |Arguments |Description
|link:#event-keyPress[keyPress] |(swiper, keyCode) |Event will be fired
on key press
|===

[role="mt-4"]
=== Mousewheel Control

lorem:sentences[3]

[role="mt-4"]
==== Mousewheel Control Parameters

lorem:sentences[3]

[cols="3a,3a,3a,3a", subs=+macros, options="header", width="100%", role="table-striped rable"]
|===
|Name |Type |Default |Description
|link:#param-mousewheel[mousewheel] |boolean \| MousewheelOptions | a|
Enables navigation through slides using mouse wheel. Object with
mousewheel parameters or boolean `true` to enable with default
settings

[source, js]
----
const swiper = new Swiper('.swiper', {
  mousewheel: {
    invert: true,
  },
});
----

|\{ | | |

|link:#param-mousewheel-enabled[enabled] |boolean |false |Set to
`true` to enable mousewheel control

|link:#param-mousewheel-eventsTarget[eventsTarget] |HTMLElement \|
'container' \| 'wrapper' \| CSSSelector |container |String with CSS
selector or HTML element of the container accepting mousewheel events.
By default it is swiper

|link:#param-mousewheel-forceToAxis[forceToAxis] |boolean |false |Set to
`true` to force mousewheel swipes to axis. So in horizontal mode
mousewheel will work only with horizontal mousewheel scrolling, and only
with vertical scrolling in vertical mode.

|link:#param-mousewheel-invert[invert] |boolean |false |Set to `true`
to invert sliding direction

|link:#param-mousewheel-noMousewheelClass[noMousewheelClass] |string
|'swiper-no-mousewheel' |Scrolling on elements with this class will be
ignored

|link:#param-mousewheel-releaseOnEdges[releaseOnEdges] |boolean |false
|Set to `true` and swiper will release mousewheel event and allow page
scrolling when swiper is on edge positions (in the beginning or in the
end)

|link:#param-mousewheel-sensitivity[sensitivity] |number |1 |Multiplier
of mousewheel data, allows to tweak mouse wheel sensitivity

|link:#param-mousewheel-thresholdDelta[thresholdDelta] |number \| null
|null |Minimum mousewheel scroll delta to trigger swiper slide change

|link:#param-mousewheel-thresholdTime[thresholdTime] |number \| null
|null |Minimum mousewheel scroll time delta (in ms) to trigger swiper
slide change

|} | | |
|===

[role="mt-4"]
==== Mousewheel Control Methods

lorem:sentences[3]

[cols=",,",]
|===
|Properties | |

|link:#prop-swiper-mousewheel-enabled[swiper.mousewheel.enabled]
|boolean |Whether the mousewheel control is enabled

|Methods | |

|link:#method-swiper-mousewheel-disable[swiper.mousewheel.disable()] |
|Disable mousewheel control

|link:#method-swiper-mousewheel-enable[swiper.mousewheel.enable()] |
|Enable mousewheel control
|===

[role="mt-4"]
==== Mousewheel Events

lorem:sentences[3]

[cols=",,",subs=+macros, options="header",]
|===
|Name |Arguments |Description
|link:#event-scroll[scroll] |(swiper, event) |Event will be fired on
mousewheel scroll
|===


[role="mt-4"]
=== Virtual Slides

Virtual Slides module allows to keep just required amount of slides in
DOM. It is very useful in terms in performance and memory issues if you
have a lot of slides, especially slides with heavyweight DOM tree or
images.

Note that according to Virtual Slides realization it *doesn't work* with
Grid module and `slidesPerView: 'auto`

[role="mt-4"]
==== Virtual Slides Parameters

lorem:sentences[3]

[cols="3a,3a,3a,3a", subs=+macros, options="header", width="100%", role="table-striped rable"]
|===
|Name |Type |Default |Description
|link:#param-virtual[virtual] |boolean \| VirtualOptions | a|
Enables virtual slides functionality. Object with virtual slides
parameters or boolean `true` to enable with default settings.

[source, js]
----
const swiper = new Swiper('.swiper', {
  virtual: {
    slides: ['Slide 1', 'Slide 2', 'Slide 3', 'Slide 4', 'Slide 5'],
  },
});
----

|\{ | | |

|link:#param-virtual-addSlidesAfter[addSlidesAfter] |number |0
|Increases amount of pre-rendered slides after active slide

|link:#param-virtual-addSlidesBefore[addSlidesBefore] |number |0
|Increases amount of pre-rendered slides before active slide

|link:#param-virtual-cache[cache] |boolean |true |Enables DOM cache of
rendering slides html elements. Once they are rendered they will be
saved to cache and reused from it.

|link:#param-virtual-enabled[enabled] |boolean |false |Whether the
virtual slides are enabled

|link:#param-virtual-renderExternal[renderExternal]
|function(VirtualData) |null a|
Function for external rendering (e.g. using some other library to handle
DOM manipulations and state like React.js or Vue.js). As an argument it
accepts `data` object with the following properties:

* `offset` - slides left/top offset in px
* `from` - index of first slide required to be rendered
* `to` - index of last slide required to be rendered
* `slides` - array with slide items to be rendered

|link:#param-virtual-renderExternalUpdate[renderExternalUpdate] |boolean
|true |When enabled (by default) it will update Swiper layout right
after renderExternal called. Useful to disable and update swiper
manually when used with render libraries that renders asynchronously

|link:#param-virtual-renderSlide[renderSlide] |function(T, any) |null
|Function to render slide. As an argument it accepts current slide item
for `slides` array and index number of the current slide. Function
must return an outer HTML of the swiper slide or slide HTML element.

|link:#param-virtual-slides[slides] |T[] |[] |Array with slides

|link:#param-virtual-slidesPerViewAutoSlideSize[slidesPerViewAutoSlideSize]
|number |320 |Slide size for slidesPerView: `auto` (in px)

|} | | |
|===

[role="mt-4"]
==== Virtual Slides Methods

lorem:sentences[3]

[width="100%",cols="34%,33%,33%",]
|===
|Properties | |

|link:#prop-swiper-virtual-cache[swiper.virtual.cache] |object |Object
with cached slides HTML elements

|link:#prop-swiper-virtual-from[swiper.virtual.from] |number |Index of
first rendered slide

|link:#prop-swiper-virtual-slides[swiper.virtual.slides]
T[]# |Array with slide items passed by `virtual.slides`
parameter

|link:#prop-swiper-virtual-to[swiper.virtual.to] |number |Index of last
rendered slide

|Methods | |

|link:#method-swiper-virtual-appendSlide[swiper.virtual.appendSlide([.text-orange]#slide#)]
| a|
Append slide. `slides` can be a single slide item or array with such
slides.

____
Only for Core version (in React & Vue it should be done by modifying
slides array/data/source)
____

|link:#method-swiper-virtual-prependSlide[swiper.virtual.prependSlide([.text-orange]#slide#)]
| a|
Prepend slide. `slides` can be a single slide item or array with such
slides.

____
Only for Core version (in React & Vue it should be done by modifying
slides array/data/source)
____

|link:#method-swiper-virtual-removeAllSlides[swiper.virtual.removeAllSlides()]
| a|
Remove all slides

____
Only for Core version (in React & Vue it should be done by modifying
slides array/data/source)
____

|link:#method-swiper-virtual-removeSlide[swiper.virtual.removeSlide([.text-orange]#slideIndexes#)]
| a|
Remove specific slide or slides. `slideIndexes` can be a number with
slide index to remove or array with indexes.

____
Only for Core version (in React & Vue it should be done by modifying
slides array/data/source)
____

|link:#method-swiper-virtual-update[swiper.virtual.update([.text-orange]#force#)]
| |Update virtual slides state
|===

[role="mt-4"]
==== Virtual Slides DOM

Since version 9, Swiper virtual slides can work with slides originally
rendered in DOM. On initialize it will remove them from DOM, cache and
then re-use the ones which are required:

[source, html]
----
<div class="swiper">
  <div class="swiper-wrapper">
    <div class="swiper-slide">Slide 1</div>
    <div class="swiper-slide">Slide 2</div>
    ...
    <div class="swiper-slide">Slide 100</div>
  </div>
</div>
<script>
  const swiper = new Swiper('.swiper', {
    virtual: {
      enabled: true,
    },
  });
</script>
----


[role="mt-4"]
=== Hash Navigation

Hash navigation is intended to have a link to specific slide that allows
to load page with specific slide opened.

To make it work, you need to enable it by passing
`hashNavigation:true` parameter and adding slides hashes in
`data-hash` attribute:

[source, html]
----
<div class="swiper">
  <div class="swiper-wrapper">
    <div class="swiper-slide" data-hash="slide1">Slide 1</div>
    <div class="swiper-slide" data-hash="slide2">Slide 2</div>
    <div class="swiper-slide" data-hash="slide3">Slide 3</div>
    <div class="swiper-slide" data-hash="slide4">Slide 4</div>
    <div class="swiper-slide" data-hash="slide5">Slide 5</div>
    ...
  </div>
</div>
----

[source, js]
----
const swiper = new Swiper('.swiper', {
  //enable hash navigation
  hashNavigation: true,
});
----

[role="mt-4"]
==== Hash Navigation Parameters

lorem:sentences[3]

[cols="3a,3a,3a,3a", subs=+macros, options="header", width="100%", role="table-striped rable"]
|===
|Name |Type |Default |Description
|link:#param-hashNavigation[hashNavigation] |boolean \|
HashNavigationOptions | a|
Enables hash url navigation to for slides. Object with hash navigation
parameters or boolean `true` to enable with default settings

[source, js]
----
const swiper = new Swiper('.swiper', {
  hashNavigation: {
    replaceState: true,
  },
});
----

|\{ | | |

|link:#param-hashNavigation-getSlideIndex[getSlideIndex]
|function(Swiper, string) | |Designed to be used with Virtual slides
when it is impossible to find slide in DOM by hash (e.g. not yet
rendered)

|link:#param-hashNavigation-replaceState[replaceState] |boolean |false
|Works in addition to hashnav to replace current url state with the new
one instead of adding it to history

|link:#param-hashNavigation-watchState[watchState] |boolean |false |Set
to `true` to enable also navigation through slides (when hashnav is
enabled) by browser history or by setting directly hash on document
location

|} | | |
|===

[role="mt-4"]
==== Hash Navigation Events

lorem:sentences[3]

[cols=",,",subs=+macros, options="header",]
|===
|Name |Arguments |Description
|link:#event-hashChange[hashChange] |(swiper) |Event will be fired on
window hash change

|link:#event-hashSet[hashSet] |(swiper) |Event will be fired when swiper
updates the hash
|===


[role="mt-4"]
=== History Navigation

lorem:sentences[3]

[role="mt-4"]
==== History Navigation Parameters

lorem:sentences[3]

[cols="3a,3a,3a,3a", subs=+macros, options="header", width="100%", role="table-striped rable"]
|===
|Name |Type |Default |Description
|link:#param-history[history] |boolean \| HistoryOptions | a|
Enables history push state where every slide will have its own url. In
this parameter you have to specify main slides url like `"slides"` and
specify every slide url using `data-history` attribute.

Object with history navigation parameters or boolean `true` to enable
with default settings

[source, js]
----
const swiper = new Swiper('.swiper', {
  history: {
    replaceState: true,
  },
});
----

[source, html]
----
<!-- will produce "slides/slide1" url in browser history -->
<div class="swiper-slide" data-history="slide1"></div>
----

|\{ | | |

|link:#param-history-enabled[enabled] |boolean |false |Enables History
Plugin.

|link:#param-history-keepQuery[keepQuery] |boolean |false |Keep query
parameters when changing browser url.

|link:#param-history-key[key] |string |'slides' |Url key for slides

|link:#param-history-replaceState[replaceState] |boolean |false |Works
in addition to hashnav or history to replace current url state with the
new one instead of adding it to history

|link:#param-history-root[root] |string | |Swiper page root, useful to
specify when you use Swiper history mode not on root website page. For
example can be `https://my-website.com/` or
`https://my-website.com/subpage/` or `/subpage/`

|} | | |
|===


[role="mt-4"]
=== Controller

lorem:sentences[3]

[role="mt-4"]
==== Controller Parameters

lorem:sentences[3]

[cols="3a,3a,3a,3a", subs=+macros, options="header", width="100%", role="table-striped rable"]
|===
|Name |Type |Default |Description
|link:#param-controller[controller] |ControllerOptions | a|
Object with controller parameters or boolean `true` to enable with
default settings

[source, js]
----
const swiper = new Swiper('.swiper', {
  controller: {
    inverse: true,
  },
});
----

|\{ | | |

|link:#param-controller-by[by] |'slide' \| 'container' |slide |Defines a
way how to control another slider: slide by slide (with respect to other
slider's grid) or depending on all slides/container (depending on total
slider percentage).

|link:#param-controller-control[control] |string \| Swiper \| Swiper[]
\| HTMLElement \| null | |Pass here another Swiper instance or array
with Swiper instances that should be controlled by this Swiper. Also
accepts string with CSS selector of Swiper element, or HTMLElement of
Swiper element

|link:#param-controller-inverse[inverse] |boolean |false |Set to
`true` and controlling will be in inverse direction

|} | | |
|===

[role="mt-4"]
==== Controller Methods

lorem:sentences[3]

[cols=",,",]
|===
|Properties | |

|link:#prop-swiper-controller-control[swiper.controller.control] |Swiper
\| Swiper[] |Pass here another Swiper instance or array with Swiper
instances that should be controlled by this Swiper
|===


[role="mt-4"]
=== Accessibility (a11y)

A module that makes assistive technologies like screen readers and
keyboard users accessible. The term "a11y" is the common abbreviation
for "accessibility" (11 letters between a and y).

[role="mt-4"]
==== Accessibility Parameters

Once the A11y module is activated, Swiper automatically adds ARIA attributes
and semantic roles to the DOM. Specifically, important ARIA attributes such
as aria-live, aria-label, and role are automatically added to the slider and
its controls so that screen reader users can understand the purpose of the
slider and navigate between the slides.

Additionally, the module adds role="region" and an aria-label to the carousel
container, eliminating the need to set these ARIA attributes manually.

[cols="2a,3a,3a,3a", subs=+macros, options="header", width="100%", role="table-striped rable"]
|===
|Name |Type |Default |Description

|`a11y`
|boolean \| A11yOptions
|none
|Object with a11y parameters or boolean `true` to enable with default
settings.

[source, js]
----
const swiper = new Swiper('.swiper', {
  a11y: {
    prevSlideMessage: 'Previous slide',
    nextSlideMessage: 'Next slide',
  },
});
----

|===


[cols="3a,3a,3a,3a", subs=+macros, options="header", width="100%", role="table-striped rable"]
|===
|Name |Type |Default |Description

|link:#param-a11y-containerMessage[containerMessage] |string \| null
|'null' |Message for screen readers for outer swiper container

|link:#param-a11y-containerRole[containerRole] |string \| null |'null'
|Value of the "role" attribute to be set on the swiper container

|link:#param-a11y-containerRoleDescriptionMessage[containerRoleDescriptionMessage]
|string \| null |'null' |Message for screen readers describing the role
of outer swiper container

|link:#param-a11y-enabled[enabled] |boolean |true |Enables A11y

|link:#param-a11y-firstSlideMessage[firstSlideMessage] |string |'This is
the first slide' |Message for screen readers for previous button when
swiper is on first slide

|link:#param-a11y-id[id] |string \| number \| null |'null' |Value of
`id` attribute to be set on swiper-wrapper. If `null` will be
generated automatically

|link:#param-a11y-itemRoleDescriptionMessage[itemRoleDescriptionMessage]
|string \| null |'null' |Message for screen readers describing the role
of slide element

|link:#param-a11y-lastSlideMessage[lastSlideMessage] |string |'This is
the last slide' |Message for screen readers for next button when swiper
is on last slide

|link:#param-a11y-nextSlideMessage[nextSlideMessage] |string |'Next
slide' |Message for screen readers for next button

|link:#param-a11y-notificationClass[notificationClass] |string
|'swiper-notification' |CSS class name of A11y notification

|link:#param-a11y-paginationBulletMessage[paginationBulletMessage]
|string |'Go to slide \{\{index}}' |Message for screen readers for
single pagination bullet

|link:#param-a11y-prevSlideMessage[prevSlideMessage] |string |'Previous
slide' |Message for screen readers for previous button

|link:#param-a11y-scrollOnFocus[scrollOnFocus] |boolean |true |Enables
scrolling to the slide that has been focused

|link:#param-a11y-slideLabelMessage[slideLabelMessage] |string
|'\{\{index}} / \{\{slidesLength}}' |Message for screen readers
describing the label of slide element

|link:#param-a11y-slideRole[slideRole] |string |'group' |Value of swiper
slide `role` attribute

|link:#param-a11y-wrapperLiveRegion[wrapperLiveRegion] |boolean |true
|Whether or not the swiper-wrapper should have the `aria-live`
attribute applied to it. If true, the value will be `off` when
autoplay is enabled, otherwise it will be `polite`

|} | | |
|===


[role="mt-5"]
== Custom Build

You have two options of making custom version of Swiper.

=== Using JS Modules

If you use bundler with JS modules support in your project you can
import only the modules you need:

[source, js]
----
// Import Swiper and modules
import Swiper from 'swiper';
import { Navigation, Pagination, Scrollbar } from 'swiper/modules';

// Now you can use Swiper
const swiper = new Swiper('.swiper', {
  // Install modules
  modules: [Navigation, Pagination, Scrollbar],
  speed: 500,
  navigation: {
    nextEl: '.swiper-button-next',
    prevEl: '.swiper-button-prev',
  },
  // ...
});
----

The following modules are exported:

* `Virtual` - Virtual Slides module
* `Keyboard` - Keyboard Control module
* `Mousewheel` - Mousewheel Control module
* `Navigation` - Navigation module
* `Pagination` - Pagination module
* `Scrollbar` - Scrollbar module
* `Parallax` - Parallax module
* `FreeMode` - Free Mode module
* `Grid` - Grid module
* `Manipulation` - Slides manipulation module (only for Core version)
* `Zoom` - Zoom module
* `Controller` - Controller module
* `A11y` - Accessibility module
* `History` - History Navigation module
* `HashNavigation` - Hash Navigation module
* `Autoplay` - Autoplay module
* `EffectFade` - Fade Effect module
* `EffectCube` - Cube Effect module
* `EffectFlip` - Flip Effect module
* `EffectCoverflow` - Coverflow Effect module
* `EffectCards` - Cards Effect module
* `EffectCreative` - Creative Effect module
* `Thumbs` - Thumbs module

=== Using Build Script

Swiper comes with gulp builder that allows to build custom library
version where you may include only required modules. We need the
following:

. Download and unzip https://github.com/nolimits4web/swiper[Swiper
GitHub repository] to local folder
. Install Node.js (if not installed)
. Now, we need to install required dependencies. Go to the folder with
downloaded and unzipped Swiper repository and execute in terminal:
+
....
npm install
....
. Open `scripts/build-config.js` file:
+
[source, js]
----
module.exports = {
  // remove modules you don't need
  modules: [
    'virtual',
    'keyboard',
    'mousewheel',
    'navigation',
    'pagination',
    'scrollbar',
    'parallax',
    'zoom',
    'controller',
    'a11y',
    'history',
    'hash-navigation',
    'autoplay',
    'thumbs',
    'free-mode',
    'grid',
    'manipulation',
    'effect-fade',
    'effect-cube',
    'effect-flip',
    'effect-coverflow',
    'effect-creative',
    'effect-cards',
  ],
};
----
. Now, we are ready to build custom version of Swiper:
+
....
npm run build:prod
....
. That is all. Generated CSS and JS files and their minified versions
will be available in `dist/` folder.

////