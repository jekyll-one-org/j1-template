// claude - Update swiper manual #3
// claude - Update swiper manual #6
[role="mt-5"]
[[swiper-api--parameters]]
== Parameters

The SwiperJS API offers a rich collection of *parameters* that allow you
to configure almost every aspect of a slider. A parameter is just a
*key/value pair* you pass as the *second* argument to the `Swiper`
constructor (or, in J1 Template, write under the `parameters:` key of
the slider's YAML configuration).

.Initialization example
[source, js]
----
const mySwiper = new Swiper('.my-swiper-container', {
  speed:        400,
  spaceBetween: 100,
});
----

This section lists *all* available parameters in alphabetical order.
Each parameter gets its own subsection with a short description and,
where helpful, a code example. The accepted value types and the default
value are summarised in a small table at the end of each subsection.

[role="mt-4"]
=== a11y

Once the a11y (Accessibility) module is activated, Swiper automatically
adds ARIA attributes and semantic roles to the DOM. Specifically, important
ARIA attributes such as `aria-live`, `aria-label`, and `role` are
automatically added to the slider and its controls so that screen reader
users can understand the purpose of the slider and navigate between the
slides.

Object with a11y parameters or boolean `true` to enable with default
settings.

[source, js]
----
const mySwiper = new Swiper('.my-swiper-container', {
  a11y: {
    prevSlideMessage: 'Previous slide',
    nextSlideMessage: 'Next slide',
  },
});
----

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`a11y`
|boolean \| A11yOptions
|none

|===


[role="mt-4"]
=== allowSlideNext

Set to `false` to disable swiping to the next-slide direction
(to the right, or to the bottom for vertical sliders).

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`allowSlideNext`
|boolean
|true

|===

[role="mt-4"]
=== allowSlidePrev

Set to `false` to disable swiping to the previous-slide direction
(to the left, or to the top for vertical sliders).

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`allowSlidePrev`
|boolean
|true

|===

[role="mt-4"]
=== allowTouchMove

Set to `false` to disable touch/mouse dragging. The only way to switch
slides is then through external API calls such as `slidePrev()` or
`slideNext()`.

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`allowTouchMove`
|boolean
|true

|===

[role="mt-4"]
=== autoHeight

When set to `true`, the slider wrapper will adapt its height to the
height of the *currently active slide*. Useful for sliders whose slides
have *different heights* (for example, slides with images and slides
with long text).

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`autoHeight`
|boolean
|false

|===


[role="mt-4"]
=== autoplay

Object with autoplay parameters or boolean `true` to enable with default
settings.

[source, js]
----
const swiper = new Swiper('.swiper', {
 autoplay: {
   delay: 5000,
 },
});
----

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`autoplay`
|boolean \| AutoplayOptions
|none

|===

[role="mt-4"]
=== breakpoints

Allows setting different parameters for different responsive breakpoints
(screen sizes). Not all parameters can be changed in breakpoints — only
those that do not require a different layout or different logic, such as
`slidesPerView`, `slidesPerGroup`, `spaceBetween`, and `grid.rows`.

[CAUTION]
====
Parameters like `loop` and `effect` won't work inside `breakpoints`.
====

[source, js]
----
const swiper = new Swiper('.swiper', {
  // Default parameters
  slidesPerView: 1,
  spaceBetween:  10,
  // Responsive breakpoints
  breakpoints: {
    // when window width is >= 320px
    320: {
      slidesPerView: 2,
      spaceBetween:  20
    },
    // when window width is >= 480px
    480: {
      slidesPerView: 3,
      spaceBetween:  30
    },
    // when window width is >= 640px
    640: {
      slidesPerView: 4,
      spaceBetween:  40
    }
  }
})
----

[source, js]
----
const swiper = new Swiper('.swiper', {
  slidesPerView: 1,
  spaceBetween:  10,
  // using "ratio" endpoints
  breakpoints: {
    '@0.75': {
      slidesPerView: 2,
      spaceBetween:  20,
    },
    '@1.00': {
      slidesPerView: 3,
      spaceBetween:  40,
    },
    '@1.50': {
      slidesPerView: 4,
      spaceBetween:  50,
    },
  }
});
----

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`breakpoints`
|object
|none

|===

[role="mt-4"]
=== breakpointsBase (beta)

Base for breakpoints. Can be `window` or `container`. If set to `window`
(by default) then breakpoint keys mean window width. If set to `container`
then breakpoint keys are treated as the swiper container width.

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`breakpointsBase`
|CSSSelector \| `container` \| `window`
|`window`

|===

[role="mt-4"]
=== cardsEffect

Object with Cards-effect parameters.

[source, js]
----
const swiper = new Swiper('.swiper', {
  effect: 'cards',
  cardsEffect: {
    // ...
  },
});
----

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`cardsEffect`
|CardsEffectOptions
|none

|===

[role="mt-4"]
=== centerInsufficientSlides

When enabled, centers slides if the number of slides is *less than*
`slidesPerView`.

[CAUTION]
====
Not intended to be used with `loop` mode and `grid.rows`.
====

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`centerInsufficientSlides`
|boolean
|false

|===

[role="mt-4"]
=== centeredSlides

When set to `true`, the active slide will be centred — not always on the
left side.

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`centeredSlides`
|boolean
|false

|===

[role="mt-4"]
=== centeredSlidesBounds

When set to `true`, the active slide will be centred *without* adding gaps
at the beginning and end of the slider. Requires `centeredSlides: true`.

[CAUTION]
====
Not intended to be used with `loop` or `pagination`.
====

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`centeredSlidesBounds`
|boolean
|false

|===

[role="mt-4"]
=== containerModifierClass

The beginning (prefix) of the modifier CSS class that can be added to
the swiper container depending on different parameters.

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`containerModifierClass`
|string
|'swiper-'

|===

[role="mt-4"]
=== controller

Object with controller parameters or boolean `true` to enable with
default settings.

[source, js]
----
const swiper = new Swiper('.swiper', {
  controller: {
    inverse: true,
  },
});
----

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`controller`
|ControllerOptions \| boolean
|none \| true

|===

[role="mt-4"]
=== coverflowEffect

Object with Coverflow-effect parameters.

[source, js]
----
const swiper = new Swiper('.swiper', {
  effect: 'coverflow',
  coverflowEffect: {
    rotate:       30,
    slideShadows: false,
  },
});
----

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`coverflowEffect`
|CoverflowEffectOptions
|none

|===

[role="mt-4"]
=== createElements

When enabled, Swiper will automatically wrap slides with a
`swiper-wrapper` element and create the required elements for navigation,
pagination and scrollbar when they are enabled (with their respective
params object or with boolean `true`).

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`createElements`
|boolean
|false

|===

[role="mt-4"]
=== creativeEffect

Object with Creative-effect parameters.

[source, js]
----
const swiper = new Swiper('.swiper', {
  effect: 'creative',
  creativeEffect: {
    prev: {
      // will set `translateZ(-400px)` on previous slides
      translate: [0, 0, -400],
    },
    next: {
      // will set `translateX(100%)` on next slides
      translate: ['100%', 0, 0],
    },
  },
});
----

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`creativeEffect`
|CreativeEffectOptions
|none

|===

[role="mt-4"]
=== cssMode

When enabled, Swiper will use the modern *CSS Scroll Snap* API. It
doesn't support all of Swiper's features, but should potentially bring
much better performance in simple configurations.

The following features are *not supported* when `cssMode` is enabled:

* Cube effect
* `speed` parameter may have no effect
* All transition start/end related events (use `slideChange` instead)
* `slidesPerGroup` has limited support
* `simulateTouch` doesn't have effect and "dragging" with the mouse doesn't work
* `resistance` doesn't have any effect
* `allowSlidePrev` / `allowSlideNext`
* `swipeHandler`

If you use `cssMode` together with other effects (especially 3D effects),
wrap each slide's content in a `<div class="swiper-slide-transform">`
element. Any custom styles on slides (background colours, border radius,
borders, etc.) should also be set on `swiper-slide-transform` instead of
on `swiper-slide`.

[source, html]
----
<div class="swiper">
  <div class="swiper-wrapper">
    <div class="swiper-slide">
      <!-- wrap slide content with transform element -->
      <div class="swiper-slide-transform">
        ... slide content ...
      </div>
    </div>
    ...
  </div>
</div>
<script>
const swiper = new Swiper('.swiper', {
   effect:  'flip',
   cssMode: true,
 });
</script>
----

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`cssMode`
|boolean
|false

|===

[role="mt-4"]
=== cubeEffect

Object with Cube-effect parameters.

[source, js]
----
const swiper = new Swiper('.swiper', {
  effect: 'cube',
  cubeEffect: {
    slideShadows: false,
  },
});
----

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`cubeEffect`
|CubeEffectOptions
|none

|===

[role="mt-4"]
=== direction

Can be `horizontal` or `vertical` (for a vertical slider).

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`direction`
|'horizontal' \| 'vertical'
|horizontal

|===

[role="mt-4"]
=== edgeSwipeDetection

Enable to release Swiper events for swipe-back work in app. When set to
`prevent`, it will *prevent* system swipe-back navigation instead.

[CAUTION]
=====
This feature works only with "touch" events (and not pointer events),
so it will work on iOS/Android devices and won't work on Windows devices
with pointer events.
=====

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`edgeSwipeDetection`
|string \| boolean
|false

|===

[role="mt-4"]
=== edgeSwipeThreshold

Area (in px) from the left edge of the screen where touch events are
released for swipe-back in app.

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`edgeSwipeThreshold`
|number
|20

|===

[role="mt-4"]
=== effect

Transition effect. Can be one of `slide`, `fade`, `cube`, `coverflow`,
`flip`, `creative` or `cards`.

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`effect`
|string
|'slide'

|===

[role="mt-4"]
=== enabled

Whether Swiper is initially enabled. When Swiper is disabled, it will
hide all navigation elements and won't respond to any events or
interactions.

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`enabled`
|boolean
|true

|===

[role="mt-4"]
=== eventsPrefix

Event name prefix for all DOM events emitted by Swiper Element
(web component).

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`eventsPrefix`
|string
|'swiper'

|===

[role="mt-4"]
=== fadeEffect

Object with Fade-effect parameters.

[source, js]
----
const swiper = new Swiper('.swiper', {
  effect: 'fade',
  fadeEffect: {
    crossFade: true
  },
});
----

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`fadeEffect`
|FadeEffectOptions
|none

|===

[role="mt-4"]
=== flipEffect

Object with Flip-effect parameters.

[source, js]
----
const swiper = new Swiper('.swiper', {
  effect: 'flip',
  flipEffect: {
    slideShadows: false,
  },
});
----

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`flipEffect`
|FlipEffectOptions
|none

|===



// =============================================================================
// The "Additional parameters (quick reference)" table has been converted
// into individual sections (one per parameter) using the same structure
// as the existing sections (e.g. a11y, allowSlideNext). Each section
// contains a short description, an optional code example, and a table
// summarising the parameter's name, type, and default value.
// =============================================================================


[role="mt-4"]
=== focusableElements

CSS selector for focusable elements. Swiping will be disabled on such
elements when they are "focused" (for example, a `<select>` inside a
slide will receive keyboard input instead of triggering a slide change).

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`focusableElements`
|string
|'input, select, option, textarea, button, video, label'

|===


[role="mt-4"]
=== followFinger

If disabled, the slider will be animated only when you *release* it; it
will not move while you hold your finger on it.

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`followFinger`
|boolean
|true

|===


[role="mt-4"]
=== freeMode

Enables free-mode functionality. Object with free-mode parameters or
boolean `true` to enable with default settings.

[source, js]
----
const swiper = new Swiper('.swiper', {
  freeMode: true,
});

const swiper = new Swiper('.swiper', {
  freeMode: {
    enabled: true,
    sticky:  true,
  },
});
----

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`freeMode`
|boolean \| FreeModeOptions
|none

|===


[role="mt-4"]
=== grabCursor

May slightly improve desktop usability. If `true`, the user will see the
"grab" cursor when hovering over Swiper.

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`grabCursor`
|boolean
|false

|===


[role="mt-4"]
=== grid

Object with grid parameters to enable a "multi-row" slider.

[source, js]
----
const swiper = new Swiper('.swiper', {
  grid: {
    rows: 2,
  },
});
----

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`grid`
|GridOptions
|none

|===


[role="mt-4"]
=== hashNavigation

Enables hash-URL navigation for slides. Object with hash-navigation
parameters or boolean `true` to enable with default settings.

[source, js]
----
const swiper = new Swiper('.swiper', {
  hashNavigation: {
    replaceState: true,
  },
});
----

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`hashNavigation`
|boolean \| HashNavigationOptions
|none

|===


[role="mt-4"]
=== height

Swiper height (in px). Allows you to force the Swiper height. Useful
only when you initialise Swiper when it is hidden, and in SSR and test
environments, for correct Swiper initialisation.

[CAUTION]
====
Setting this parameter will make Swiper *not* responsive.
====

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`height`
|number \| null
|null

|===


[role="mt-4"]
=== history

Enables history push-state so every slide can have its own URL. With
this parameter you specify the main slides URL like `"slides"` and
specify every slide's URL using the `data-history` attribute.

Object with history-navigation parameters or boolean `true` to enable
with default settings.

[source, js]
----
const swiper = new Swiper('.swiper', {
  history: {
    replaceState: true,
  },
});
----

[source, html]
----
<!-- will produce "slides/slide1" url in browser history -->
<div class="swiper-slide" data-history="slide1"></div>
----

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`history`
|boolean \| HistoryOptions
|none

|===


[role="mt-4"]
=== init

Whether Swiper should be initialised automatically when you create an
instance. If disabled, you need to init it manually by calling
`swiper.init()`.

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`init`
|boolean
|true

|===


[role="mt-4"]
=== initialSlide

Index number of the initial slide.

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`initialSlide`
|number
|0

|===


[role="mt-4"]
=== injectStyles

Inject text styles into the shadow DOM. Only for usage with Swiper
Element (web component).

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`injectStyles`
|string[]
|none

|===


[role="mt-4"]
=== injectStylesUrls

Inject styles `<link>` tags into the shadow DOM. Only for usage with
Swiper Element (web component).

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`injectStylesUrls`
|string[]
|none

|===


[role="mt-4"]
=== keyboard

Enables navigation through slides using the keyboard. Object with
keyboard parameters or boolean `true` to enable with default settings.

[source, js]
----
const swiper = new Swiper('.swiper', {
  keyboard: {
    enabled:        true,
    onlyInViewport: false,
  },
});
----

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`keyboard`
|boolean \| KeyboardOptions
|none

|===


[role="mt-4"]
=== lazyPreloadPrevNext

Number of next and previous slides to preload. Only applicable when
using lazy loading.

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`lazyPreloadPrevNext`
|number
|0

|===


[role="mt-4"]
=== lazyPreloaderClass

CSS class name of the lazy preloader.

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`lazyPreloaderClass`
|string
|'swiper-lazy-preloader'

|===


[role="mt-4"]
=== longSwipes

Set to `false` to disable long swipes.

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`longSwipes`
|boolean
|true

|===


[role="mt-4"]
=== longSwipesMs

Minimum duration (in ms) to trigger a swipe to the next/previous slide
during long swipes.

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`longSwipesMs`
|number
|300

|===


[role="mt-4"]
=== longSwipesRatio

Ratio to trigger a swipe to the next/previous slide during long swipes.

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`longSwipesRatio`
|number
|0.5

|===


[role="mt-4"]
=== loop

Set to `true` to enable continuous loop mode.

Because of how loop mode works (it rearranges slides), the total number
of slides must be:

* more than or equal to `slidesPerView` + `slidesPerGroup` (and `+ 1` in
  case of `centeredSlides`)
* a multiple of `slidesPerGroup` (or use `loopAddBlankSlides`)
* a multiple of `grid.rows` (or use `loopAddBlankSlides`)

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`loop`
|boolean
|false

|===


[role="mt-4"]
=== loopAddBlankSlides

Automatically adds blank slides if you use Grid or `slidesPerGroup` and
the total number of slides is not a multiple of `slidesPerGroup` or
`grid.rows`.

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`loopAddBlankSlides`
|boolean
|true

|===


[role="mt-4"]
=== loopAdditionalSlides

Allows you to increase the amount of looped slides.

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`loopAdditionalSlides`
|number
|0

|===


[role="mt-4"]
=== loopPreventsSliding

If enabled, `slideNext` / `slidePrev` will do nothing while the slider
is animating in loop mode.

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`loopPreventsSliding`
|boolean
|true

|===


[role="mt-4"]
=== maxBackfaceHiddenSlides

If the total number of slides is less than the value specified here,
Swiper will enable `backface-visibility: hidden` on slide elements to
reduce visual "flicker" in Safari.

[CAUTION]
====
It is *not* recommended to enable this on a large number of slides
because it will reduce performance.
====

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`maxBackfaceHiddenSlides`
|number
|10

|===


[role="mt-4"]
=== modules

Array of Swiper modules. Use this option to attach optional Swiper
modules (such as `Navigation`, `Pagination`, `Keyboard`, etc.) when you
import Swiper as ES modules.

[source, js]
----
import Swiper from 'swiper';
import { Navigation, Pagination } from 'swiper/modules';

const swiper = new Swiper('.swiper', {
  modules: [ Navigation, Pagination ],
});
----

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`modules`
|SwiperModule[]
|none

|===


[role="mt-4"]
=== mousewheel

Enables navigation through slides using the mouse wheel. Object with
mousewheel parameters or boolean `true` to enable with default
settings.

[source, js]
----
const swiper = new Swiper('.swiper', {
  mousewheel: {
    invert: true,
  },
});
----

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`mousewheel`
|boolean \| MousewheelOptions
|none

|===


[role="mt-4"]
=== navigation

Object with navigation parameters or boolean `true` to enable with
default settings.

[source, js]
----
const swiper = new Swiper('.swiper', {
  navigation: {
    nextEl: '.swiper-button-next',
    prevEl: '.swiper-button-prev',
  },
});
----

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`navigation`
|boolean \| NavigationOptions
|none

|===


[role="mt-4"]
=== nested

Set to `true` on the inner Swiper for correct touch-events interception.
Use only on swipers that move in the *same direction* as the parent.

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`nested`
|boolean
|false

|===


[role="mt-4"]
=== noSwiping

Enable/disable swiping on elements that match the class specified in
`noSwipingClass`.

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`noSwiping`
|boolean
|true

|===


[role="mt-4"]
=== noSwipingClass

CSS class for `noSwiping` elements.

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`noSwipingClass`
|string
|'swiper-no-swiping'

|===


[role="mt-4"]
=== noSwipingSelector

Can be used instead of `noSwipingClass` to specify elements to disable
swiping on. For example, `input` will disable swiping on all input
fields.

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`noSwipingSelector`
|string
|none

|===


[role="mt-4"]
=== normalizeSlideIndex

// claude - Update swiper manual #6
// The original description ("Normalise the slide index.") was
// not helpful to beginners. Replaced with a plain explanation of
// what "normalise" means in this context.
When enabled (default), Swiper keeps the active slide index inside
the valid range of slides. If a user action would land on an
out-of-range index, Swiper rounds it back to the closest real slide
index. Leave this on unless you have a very specific reason to
disable it.

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`normalizeSlideIndex`
|boolean
|true

|===


[role="mt-4"]
=== observeParents

Set to `true` if you also need to watch mutations on Swiper *parent*
elements.

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`observeParents`
|boolean
|false

|===


[role="mt-4"]
=== observeSlideChildren

Set to `true` if you also need to watch mutations on Swiper *slide
children* elements.

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`observeSlideChildren`
|boolean
|false

|===


[role="mt-4"]
=== observer

Set to `true` to enable Mutation Observer on Swiper and its elements.
In this case Swiper will be updated (re-initialised) each time you
change its style (such as hide/show) or modify its child elements
(such as adding or removing slides).

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`observer`
|boolean
|false

|===


[role="mt-4"]
=== on

Register event handlers. Pass an object whose keys are Swiper event
names and whose values are the handler functions to invoke when those
events fire.

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`on`
|object
|none

|===


[role="mt-4"]
=== onAny

Add an event listener that will be fired on *all* events.

[source, js]
----
const swiper = new Swiper('.swiper', {
  onAny(eventName, ...args) {
    console.log('Event: ',      eventName);
    console.log('Event data: ', args);
  }
});
----

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`onAny`
|function(handler)
|none

|===


[role="mt-4"]
=== oneWayMovement

When enabled, slides will only swipe *forward* (one way) regardless of
the swipe direction.

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`oneWayMovement`
|boolean
|false

|===


[role="mt-4"]
=== pagination

Object with pagination parameters or boolean `true` to enable with
default settings.

[source, js]
----
const swiper = new Swiper('.swiper', {
  pagination: {
    el:   '.swiper-pagination',
    type: 'bullets',
  },
});
----

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`pagination`
|boolean \| PaginationOptions
|none

|===


[role="mt-4"]
=== parallax

Object with parallax parameters or boolean `true` to enable with
default settings.

[source, js]
----
const swiper = new Swiper('.swiper', {
  parallax: true,
});
----

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`parallax`
|boolean \| ParallaxOptions
|none

|===


[role="mt-4"]
=== passiveListeners

// claude - Update swiper manual #6
// Plain-English rewrite. The original referred to "passive event
// listeners" and `e.preventDefault` without explaining what they
// are; beginners reading the J1 manual could not act on that text.
Browsers can attach touch/scroll event listeners in a *passive* mode,
which lets the page keep scrolling smoothly while Swiper handles
gestures. Swiper turns this on by default. Set this parameter to
`false` only if you call `event.preventDefault()` inside your own
Swiper event handlers and notice that it has no effect.

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`passiveListeners`
|boolean
|true

|===


[role="mt-4"]
=== preventClicks

Set to `true` to prevent accidental unwanted clicks on links during
swiping.

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`preventClicks`
|boolean
|true

|===


[role="mt-4"]
=== preventClicksPropagation

Set to `true` to stop click-event propagation on links during swiping.

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`preventClicksPropagation`
|boolean
|true

|===


[role="mt-4"]
=== preventInteractionOnTransition

When enabled, it won't allow you to change slides by swiping or by
navigation/pagination buttons during a transition.

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`preventInteractionOnTransition`
|boolean
|false

|===


[role="mt-4"]
=== resistance

Set to `false` if you want to disable resistant bounds.

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`resistance`
|boolean
|true

|===


[role="mt-4"]
=== resistanceRatio

// claude - Update swiper manual #6
// Replaced the circular description ("Controls the resistance ratio")
// with a concrete explanation that tells the reader what numbers do.
How "elastic" the slider feels when you try to drag past the first or
last slide. A value of `0` means the slider stops dead at the edges
(no bounce); a value of `1` means it follows the finger 1:1 even
past the edge. The default of `0.85` gives a gentle resistance.

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`resistanceRatio`
|number
|0.85

|===


[role="mt-4"]
=== resizeObserver

When enabled, Swiper will use ResizeObserver (if supported by the
browser) on the swiper container to detect container resize (instead of
watching for window resize).

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`resizeObserver`
|boolean
|true

|===


[role="mt-4"]
=== rewind

Set to `true` to enable "rewind" mode. When enabled, clicking the
"next" navigation button (or calling `.slideNext()`) on the last slide
will slide back to the first slide. Clicking "prev" (or calling
`.slidePrev()`) on the first slide will slide forward to the last
slide.

[CAUTION]
====
Should not be used together with `loop` mode.
====

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`rewind`
|boolean
|false

|===


[role="mt-4"]
=== roundLengths

Set to `true` to round the values of slide width and height to prevent
blurry text on usual-resolution screens (if you have such).

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`roundLengths`
|boolean
|false

|===


[role="mt-4"]
=== runCallbacksOnInit

Fire Transition/SlideChange/Start/End events on Swiper initialisation.
Such events will be fired on initialisation when your `initialSlide` is
not `0`, or when you use loop mode.

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`runCallbacksOnInit`
|boolean
|true

|===


[role="mt-4"]
=== scrollbar

Object with scrollbar parameters or boolean `true` to enable with
default settings.

[source, js]
----
const swiper = new Swiper('.swiper', {
  scrollbar: {
    el:        '.swiper-scrollbar',
    draggable: true,
  },
});
----

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`scrollbar`
|boolean \| ScrollbarOptions
|none

|===


[role="mt-4"]
=== setWrapperSize

Enable this option and the plugin will set width/height on the swiper
wrapper equal to the total size of all slides. Mostly used as a
compatibility fallback for browsers that don't support flexbox layout
well.

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`setWrapperSize`
|boolean
|false

|===


[role="mt-4"]
=== shortSwipes

Set to `false` to disable short swipes.

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`shortSwipes`
|boolean
|true

|===


[role="mt-4"]
=== simulateTouch

If `true`, Swiper will accept mouse events like touch events (click
and drag to change slides).

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`simulateTouch`
|boolean
|true

|===


[role="mt-4"]
=== slideActiveClass

CSS class name of the currently active slide.

[CAUTION]
====
By changing classes you will also need to change Swiper's CSS to reflect
the changed classes.
====


[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`slideActiveClass`
|string
|'swiper-slide-active'

|===


[role="mt-4"]
=== slideBlankClass

CSS class name of the blank slide added by loop mode (when
`loopAddBlankSlides` is enabled).

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`slideBlankClass`
|string
|'swiper-slide-blank'

|===


[role="mt-4"]
=== slideClass

CSS class name of a slide.

[CAUTION]
====
By changing classes you will also need to change Swiper's CSS to reflect
the changed classes.
====

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`slideClass`
|string
|'swiper-slide'

|===


[role="mt-4"]
=== slideFullyVisibleClass

CSS class name of a fully visible slide (when the whole slide is in the
viewport).

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`slideFullyVisibleClass`
|string
|'swiper-slide-fully-visible'

|===


[role="mt-4"]
=== slideNextClass

CSS class name of the slide that is right *after* the currently active
slide.

[CAUTION]
====
By changing classes you will also need to change Swiper's CSS to reflect
the changed classes.
====

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`slideNextClass`
|string
|'swiper-slide-next'

|===


[role="mt-4"]
=== slidePrevClass

CSS class name of the slide that is right *before* the currently active
slide.

[CAUTION]
====
By changing classes you will also need to change Swiper's CSS to reflect
the changed classes.
====

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`slidePrevClass`
|string
|'swiper-slide-prev'

|===


[role="mt-4"]
=== slideToClickedSlide

Set to `true` and a click on any slide will produce a transition to
that slide.

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`slideToClickedSlide`
|boolean
|false

|===


[role="mt-4"]
=== slideVisibleClass

CSS class name of a currently/partially visible slide.

[CAUTION]
====
By changing classes you will also need to change Swiper's CSS to reflect
the changed classes.
====

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`slideVisibleClass`
|string
|'swiper-slide-visible'

|===


[role="mt-4"]
=== slidesOffsetAfter

Add (in px) additional slide offset at the *end* of the container
(after all slides).

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`slidesOffsetAfter`
|number
|0

|===


[role="mt-4"]
=== slidesOffsetBefore

Add (in px) additional slide offset at the *beginning* of the container
(before all slides).

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`slidesOffsetBefore`
|number
|0

|===


[role="mt-4"]
=== slidesPerGroup

Set the number of slides to define and enable group sliding. Useful
with `slidesPerView > 1`.

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`slidesPerGroup`
|number
|1

|===


[role="mt-4"]
=== slidesPerGroupAuto

Intended to be used only with `slidesPerView: 'auto'` and
`slidesPerGroup: 1`. When enabled, it will skip all slides in view on
`.slideNext()` and `.slidePrev()` method calls, on Navigation "buttons"
clicks, and during autoplay.

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`slidesPerGroupAuto`
|boolean
|false

|===


[role="mt-4"]
=== slidesPerGroupSkip

The parameter works in the following way: if `slidesPerGroupSkip`
equals `0` (default), no slides are excluded from grouping, and the
resulting behaviour is the same as without this change.

If `slidesPerGroupSkip` is equal to or greater than `1`, the first *X*
slides are treated as single groups, whereas all following slides are
grouped by the `slidesPerGroup` value.

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`slidesPerGroupSkip`
|number
|0

|===


[role="mt-4"]
=== slidesPerView

Number of slides per view (slides visible at the same time on the
slider's container).

[CAUTION]
====
`slidesPerView: 'auto'` is currently *not* compatible with multi-row
mode (when `grid.rows > 1`).
====

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`slidesPerView`
|number \| 'auto'
|1

|===


// claude - Update swiper manual #6
// The `snapToSlideEdge` parameter was removed from this manual:
// it is *not* part of the Swiper 11.2.5 bundle shipped with the
// J1 Template (verified against `swiper-bundle.js`). It first
// appears in Swiper 12.x and would therefore confuse beginners.

[role="mt-4"]
=== spaceBetween

Distance between slides in px.

[CAUTION]
====
If you use a `margin` CSS property on the elements that go into Swiper,
and at the same time pass `spaceBetween`, navigation might not work
properly.
====

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`spaceBetween`
|string \| number
|0

|===


[role="mt-4"]
=== speed

Duration of the transition between slides (in ms).

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`speed`
|number
|300

|===


[role="mt-4"]
=== swipeHandler

String with the CSS selector or HTML element of the container with
pagination that will work as the only available handler for swiping.

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`swipeHandler`
|HTMLElement \| CSSSelector \| null
|null

|===


[role="mt-4"]
=== swiperElementNodeName

The name of the swiper-element node; used for detecting web-component
rendering.

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`swiperElementNodeName`
|string
|'SWIPER-CONTAINER'

|===


[role="mt-4"]
=== threshold

Threshold value in px. If the "touch distance" is lower than this
value, swiper will not move.

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`threshold`
|number
|5

|===


[role="mt-4"]
=== thumbs

Object with thumbs-component parameters. Used to control a separate
"thumbnails" Swiper instance that drives the main slider.

[source, js]
----
const swiper = new Swiper('.swiper', {
  thumbs: {
    swiper: thumbsSwiper
  }
});
----

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`thumbs`
|ThumbsOptions
|none

|===


[role="mt-4"]
=== touchAngle

Allowable angle (in degrees) to trigger a touch move.

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`touchAngle`
|number
|45

|===


[role="mt-4"]
=== touchEventsTarget

Target element to listen for touch events on. Can be `container` (to
listen for touch events on the swiper) or `wrapper` (to listen for
touch events on the swiper-wrapper).

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`touchEventsTarget`
|'container' \| 'wrapper'
|wrapper

|===


[role="mt-4"]
=== touchMoveStopPropagation

If enabled, propagation of `touchmove` will be stopped.

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`touchMoveStopPropagation`
|boolean
|false

|===


[role="mt-4"]
=== touchRatio

Touch ratio. A value greater than `1` makes slides follow the finger
faster than the actual movement, a value lower than `1` makes them
follow more slowly.

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`touchRatio`
|number
|1

|===


[role="mt-4"]
=== touchReleaseOnEdges

Enable to release touch events at the slider edge position (beginning,
end) to allow for further page scrolling.

[CAUTION]
====
This feature works only with "touch" events (and not pointer events),
so it will work on iOS/Android devices and won't work on Windows devices
with pointer events. The `threshold` parameter must also be set to `0`.
====

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`touchReleaseOnEdges`
|boolean
|false

|===


[role="mt-4"]
=== touchStartForcePreventDefault

Force to always prevent default for `touchstart` (`pointerdown`) events.

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`touchStartForcePreventDefault`
|boolean
|false

|===


[role="mt-4"]
=== touchStartPreventDefault

If disabled, the `pointerdown` event won't be prevented.

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`touchStartPreventDefault`
|boolean
|true

|===


[role="mt-4"]
=== uniqueNavElements

If enabled (by default) and navigation-element parameters are passed as
a string (such as `".pagination"`), Swiper will look for such elements
among child elements first. Applies to pagination, prev/next buttons
and scrollbar elements.

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`uniqueNavElements`
|boolean
|true

|===


[role="mt-4"]
=== updateOnWindowResize

Swiper will recalculate slides position on window resize
(`orientationchange`).

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`updateOnWindowResize`
|boolean
|true

|===


[role="mt-4"]
=== url

Required for active-slide detection when rendered on server-side and
when history is enabled.

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`url`
|string \| null
|null

|===


[role="mt-4"]
=== userAgent

`userAgent` string. Required for browser/device detection when rendered
on server-side.

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`userAgent`
|string \| null
|null

|===


[role="mt-4"]
=== virtual

Enables virtual-slides functionality. Object with virtual-slides
parameters or boolean `true` to enable with default settings.

[source, js]
----
const swiper = new Swiper('.swiper', {
  virtual: {
    slides: ['Slide 1', 'Slide 2', 'Slide 3', 'Slide 4', 'Slide 5'],
  },
});
----

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`virtual`
|boolean \| VirtualOptions
|none

|===


[role="mt-4"]
=== virtualTranslate

Enable this option and Swiper will be operated as usual except that it
will not move — real translate values on the wrapper will not be set.
Useful when you need to create a custom slide transition.

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`virtualTranslate`
|boolean
|false

|===


[role="mt-4"]
=== watchOverflow

When enabled, Swiper will be disabled and hide navigation buttons in
case there are not enough slides for sliding.

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`watchOverflow`
|boolean
|true

|===


[role="mt-4"]
=== watchSlidesProgress

Enable this feature to calculate each slide's progress and visibility
(slides in the viewport will have an additional `visible` class).

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`watchSlidesProgress`
|boolean
|false

|===


[role="mt-4"]
=== width

Swiper width (in px). Allows you to force the Swiper width. Useful only
when you initialise Swiper while it is hidden, and in SSR and test
environments, for correct Swiper initialisation.

[CAUTION]
====
Setting this parameter will make Swiper *not* responsive.
====

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`width`
|number \| null
|null

|===


[role="mt-4"]
=== wrapperClass

CSS class name of the slides' wrapper.

[CAUTION]
====
By changing classes you will also need to change Swiper's CSS to reflect
the changed classes.
====

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`wrapperClass`
|string
|'swiper-wrapper'

|===


[role="mt-4"]
=== zoom

Enables zooming functionality. Object with zoom parameters or boolean
`true` to enable with default settings.

[source, js]
----
const swiper = new Swiper('.swiper', {
  zoom: {
    maxRatio: 5,
  },
});
----

[cols="3a,2a,1a", options="header", width="100%", role="rtable"]
|===
|Name |Type |Default

|`zoom`
|boolean \| ZoomOptions
|none

|===
