Prompt
  https://claude.ai/chat/d4b5e792-06f8-4e8c-b93d-56e5c0b0777a
--------------------------------------------------------------------------------

For pages generated by the J1 Template using the YouTube iframe API, in Google
Chrome/Lighthouse, they are marked as "Uses third-party cookies X cookies found.
Third-party cookies may be blocked in some contexts." for "Best Practices".
Additionally, for each cookie found, issues are logged to the Issues panel
in Chrome Devtools. This degrades the "Best Practices" score a lot (by 13/100).

Why is it?

Is it really possible to use a resource like YouTube iframe API without 
allowing cookies?

Create fixed versions and use the comment
"claude - Optimize J1 third-party cookies #1" for all proposed fixes.


Claude
--------------------------------------------------------------------------------

Now the AmplitudeJS YouTube tech (ytp.js):Now the full validation pipeline.

## Why Lighthouse flags this

The classic YouTube embed host `www.youtube.com` sets a batch of cookies
(`VISITOR_INFO1_LIVE`, `YSC`, `__Secure-*`, etc.) the moment the iframe
document loads — before any user interaction.

From your page's perspective these are third-party cookies, so Lighthouse's
"Uses third-party cookies" Best-Practices audit flags every one of them and
Chrome logs each to the Issues panel (Chrome is phasing third-party cookies
out, so the audit is weighted heavily).

A secondary contributor: your poster images come from `img.youtube.com`,
and every request to a `*.youtube.com` host carries the youtube.com cookies
too.


## Is it possible without cookies?

Yes, largely — YouTube provides the **privacy-enhanced host**
`www.youtube-nocookie.com` exactly for this. Same IFrame Player API, same
JS control surface; the difference is that the embed sets no tracking cookies
on load (state moves to interaction time / other storage).

The `iframe_api` loader script itself can stay on `www.youtube.com` — it's
a static JS resource and not the cookie setter. One honest caveat: this is
Google's mechanism, so "zero storage forever" isn't guaranteed (it may still
use localStorage, and cookies can appear after the user starts playback),
but the load-time cookie findings that cost you the 13 points disappear.

Conveniently, your videojs-youtube tech (youtube.js, lines ~300–302) already
supports this via `enablePrivacyEnhancedMode` → `playerConfig.host` — it was
just never passed by the J1 core.


## Fixes applied — tag `claude - optimize J1 third-party cookies #1`

**videoPlayer.js** (core, 4 tags)
`createVideoJsPlayer()` now passes `enablePrivacyEnhancedMode` into the
`youtube:` tech options — read from `videoJS.players.youtube.enablePrivacyEnhancedMode`
through the normal three-layer chain, **defaulting to `true`** when the YAML
key is absent (set it to `false` to restore the classic host). The three
active poster URLs (playlist normalise, `createEntry`, media record) now
use the cookieless image CDN `i.ytimg.com` instead of `img.youtube.com`
identical images, no youtube.com cookies attached.

**youtube.js** (tech, 3 tags): both poster URLs
(`mqdefault` initial + `maxresdefault` probe) switched to `i.ytimg.com`.
Plus a comment-only note at `loadScript()` documenting why the API loader
intentionally stays on `www.youtube.com`.

**ytp.js** (AmplitudeJS tech, 2 tags)
The hidden video iframe's `YT.Player` config now gets `host:`, driven by
a new per-player YAML key `yt_player.privacy_enhanced`:

  per-player ← `amplitude_default` ← hard default `'true'`

using the usual empty-string length checks since Liquid renders missing
keys as `''`.

**videojs.js**
No change needed — verified byte-identical; it has no YouTube touchpoints
(the tech options flow through `videoPlayer.yml`, not `videojs.yml`).


## One flag for your review

The YAML defaults (`videoPlayer.yml`, `amplitude.yml`) don't yet declare the
new keys — everything works via the hard defaults, but you may want to add
`enablePrivacyEnhancedMode: true` and `privacy_enhanced: true` there for
discoverability.

Out of scope here, not touched.

